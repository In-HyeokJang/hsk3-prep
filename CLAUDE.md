# HSK 3급 준비 사이트

## 프로젝트

HSK 3급을 준비하는 사람이 단어와 예문을 매일 조금씩 익히고, 스스로 확인하는 개인 학습 사이트.

**대화를 새로 시작했다면 `docs/09-handoff.md` 를 먼저 읽는다.** 지금 어디까지 왔고 다음에 뭘 하는지가 거기 있다.

## 스택

- Next.js (App Router) + TypeScript
- Tailwind CSS (색과 간격)
- Supabase (데이터 저장)
- Vercel (배포)

## 명령어

- 개발 서버 켜기: `npm run dev` (3회차부터)
- 빌드: `npm run build`

데이터베이스 (`.env.local` 의 `SUPABASE_DB_URL` 을 씁니다)

- 표 만들기: `npm run db:push` — `supabase/migrations/` 실행
- 자료 넣기: `npm run db:seed` — `data/words-*.csv` 를 읽어서 upsert
- 확인: `npm run db:check` — 줄 수 · 한자 깨짐 · 권한 · 함수까지
- 한 번에: `npm run db:all`

## 데이터베이스를 다룰 때

- **스키마를 바꿀 때는 `supabase/migrations/` 에 새 파일을 만든다.** 기존 파일을 고치지 않는다.
- **마이그레이션은 반드시 몇 번 돌려도 되게 쓴다.** `if not exists` · `or replace` · `drop ... if exists`. `db:push` 가 매번 전부 다시 실행한다.
- **`v_words` 를 다시 만들 때는 `drop view ... cascade` 를 쓴다.** 이 보기를 반환하는 함수들이 의존하고 있어서, 딸린 것을 나열해 지우는 방식은 함수를 추가할 때마다 깨진다. cascade 로 지워진 함수는 뒤쪽 마이그레이션이 다시 만든다.
- **함수의 최종 정의는 가장 나중 마이그레이션에 있어야 한다.** 앞 마이그레이션에서 cascade 로 지워지기 때문이다.
- **마이그레이션을 고치면 두 번 연속 실행해본다.** 처음 만들 때는 지울 게 없어서 지나가고, **두 번째 실행에서만 드러나는 문제**가 있다.
- **자료가 바뀌면 SQL을 새로 만들지 않는다.** `data/` 의 CSV를 고치고 `npm run db:seed` 를 다시 돌린다.
- `.env.local` 의 내용을 화면에 출력하지 않는다. 접속 문자열은 비밀번호를 담고 있다.
- `service_role` 키는 쓰지 않는다.

## 나에 대해

- 나는 코딩을 모른다. 코드를 직접 쓰지 않는다.
- 전문 용어를 쓸 거면 괄호 안에 쉬운 말로 풀어써라.

## 작업 규칙

- 한국어로 답한다.
- 설명은 짧게. 세 줄을 넘기지 않는다.
- 파일을 고치기 전에, 무엇을 왜 바꿀지 한 줄로 먼저 말한다.
- 한 번에 파일 하나씩만 고친다.
- 한 번에 하나씩만 한다. 여러 개를 시키면 하나만 하고 나머지는 되물어라.
- 새 라이브러리를 설치하기 전에 반드시 물어본다.
- 내가 요청하지 않은 파일은 건드리지 않는다.

## 하지 말 것

- `.env` 파일의 내용을 화면에 출력하지 않는다.
- API 키를 코드 안에 직접 적지 않는다. 반드시 `.env` 를 쓴다.
- 내가 이해하지 못할 것 같은 복잡한 구조를 만들지 않는다.

## 이 프로젝트만의 규칙 (중국어)

- **한자는 간체자(简体)만 쓴다.** 번체자를 섞지 않는다.
- **병음은 성조 기호를 붙여서 쓴다.** `xuéxí` 처럼. `xue2xi2` 나 성조 없는 `xuexi` 로 쓰지 않는다.
- **한자를 보여주는 곳에는 반드시 중국어 폰트를 지정한다.** 지정하지 않으면 한국어 폰트가 한자를 대신 그려서 자형이 어긋난다.
- **예문을 새로 지어내지 않는다.** 예문이 필요하면 `data/` 안의 자료에서 가져오고, 없으면 나에게 물어본다.
- **HSK 급수 정보를 기억에 의존해 쓰지 않는다.** 단어 수, 시험 구성, 급수 기준은 `docs/03-data.md` 에 적힌 것만 따른다.

## 페이지 구조

`docs/02-plan.md` 에 정리돼 있다. 화면 작업을 시작하기 전에 그 파일을 먼저 읽는다.

1. **홈 `/`** — 오늘의 단어 10개, 오늘 진도
2. **단어장 `/words`** — 전체 단어 목록, 검색, 상세 `/words/[id]`
3. **학습 `/study`** — 카드 퀴즈, 결과 저장

## 데이터 구조

DDL은 `db/01-schema.sql` 에 있다. **화면 작업을 하기 전에 그 파일을 먼저 읽는다.**

| 표 | 뭐가 |
|---|---|
| `words` | 단어. PK는 `id` (`'L3-0001'`) |
| `examples` | 예문. `word_id` + `seq`. 단어 하나에 여러 개 가능 |
| `progress` | 내 진도. `unique (user_key, word_id)` |
| `attempts` | 푼 기록. 쌓기만 함 |
| `v_words` | **보기.** 단어 + 대표 예문(seq=1)을 한 줄로 |

### 데이터를 다룰 때 지킬 것

- **화면에서 단어를 읽을 때는 `v_words` 를 쓴다.** `words` 와 `examples` 를 직접 붙이지 않는다.
- **단어를 가리키는 칸 이름은 `word_id` 다.** `words` 표 안에서만 `id`.
- **단어의 기본 키는 `hanzi` 가 아니다.** 공식 목록에 같은 한자가 두 번 나온다 (把 개사/양사, 背 bēi/bèi). 한자로 묶으면 단어가 에러 없이 조용히 사라진다.
- **진도는 `upsert` + `onConflict` 로 저장한다.** `onConflict` 를 빼면 기본 키(`id`) 기준이 되어 두 번째 클릭에 `409 duplicate key` 가 난다. 실제로 확인된 동작이다.
  ```js
  supabase.from('progress').upsert(row, { onConflict: 'user_key,word_id' })
  ```
- **권한에 막힌 요청은 에러가 아니라 `204` 로 돌아온다.** 상태 코드로 성공을 판단하지 않는다. 바꿨다면 다시 읽어서 확인한다.
- **저장 성공은 서버 응답으로 판단한다.** 에러 없이 끝난 것과 저장된 것은 다르다.
- **`words` 와 `examples` 는 사이트에서 고치지 않는다.** anon 권한이 읽기 전용이다.
- **`user_key` 는 브라우저에 저장한 UUID다.** 로그인을 만들지 않는다.
- 병음 검색은 `pinyin_plain` 으로 한다. 성조 기호를 뗀 값이 자동으로 들어 있다.
- 자료를 다시 넣을 때는 표를 지우지 말고 시드 SQL을 다시 실행한다. `on conflict do update` 라 몇 번을 돌려도 같다.
- 새 칸이 필요해 보이면 먼저 물어본다. 대부분 `meta jsonb` 로 해결된다.
