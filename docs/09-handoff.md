# 새 대화에서 이어가기

> **`/clear` 를 하면 지금까지 나눈 대화가 사라집니다.**
> 이 파일은 그때 잃어버리면 안 되는 것만 적어둔 곳입니다.
>
> 마지막 갱신: 2026-08-04 (2회차 작업 끝) · 단어 973/973 · 검수 0

---

## 지금 어디까지 왔나

| | 상태 |
|---|---|
| 자료 | 공식 3급 973단어 · **한국어 뜻과 예문 973개 전부 완료** |
| 데이터베이스 | 표 5개 · 보기 2개 · 마이그레이션 14개 · 권한 설정 완료 |
| 사이트 | 화면 4개 + 로그인 화면 · 반응형 · PWA · 밝은/어두운 화면 |
| 학습 | **3가지 퀴즈** (뜻 쓰기 / 뜻 고르기 / 한자 고르기) + 간격 반복 복습 |
| 계정 | **아이디 회원가입 · 로그인 · 탈퇴 · 아이디 찾기** |
| 깃허브 | https://github.com/In-HyeokJang/hsk3-prep · `main` + `dev` |
| 배포 | **아직** |
| 검수 | **아직 0개** |

---

## 새 대화를 열면 이 말부터

```
c:\workspace\hsk3-prep 프로젝트를 이어서 할 거야.
docs/09-handoff.md 를 먼저 읽고, 지금 상태를 확인해줘.
```

그러면 알아서 `npm run db:check` 를 돌려서 지금 몇 개인지 확인합니다.

---

## 오늘(2회차) 무엇이 바뀌었나

### 1. 단어를 다 채웠습니다 (600 → 973)

`data/words-l3-freq-601-700.csv` 부터 `901-973.csv` 까지 4개를 더 만들었습니다.
마지막 6개는 단어가 아니라 접사입니다 (`初（初一）`, `…极了`, `者（志愿者）` 등).

### 2. 학습이 퀴즈로 바뀌었습니다

예전에는 카드를 뒤집어 뜻을 보는 방식이었는데, 이제 문제를 냅니다.

| 뜻이 | 어떻게 나오나 |
|---|---|
| 짧고 하나 (8자 이하, `~` 없음) | **직접 입력**. `世界` → "세계" 를 칩니다 |
| 길거나 여러 개 | **4지선다**. 한 문제씩 번갈아 한자→뜻 / 뜻→한자 |

규칙은 `web/lib/quiz.ts` 한 곳에 모아뒀습니다. 채점은 너그럽습니다 —
`계속하다` 의 정답에 "계속" 만 써도 맞다고 합니다.

### 3. 반복 복습 (간격 반복)

한 번 맞히면 영영 안 나오던 것을 고쳤습니다.

```
연속 1번 맞힘 →  1일 뒤      연속 4번 →  16일 뒤
연속 2번      →  3일 뒤      그 뒤로   →  35일 뒤
연속 3번      →  7일 뒤      틀리면    →  3분 뒤부터
```

연속 정답 횟수는 새 칸을 만들지 않고 `progress.meta` 안에 `streak` 로 넣었습니다.

### 4. 로그인이 생겼습니다

**아이디로 가입합니다.** 이메일이 아닙니다.

- 가입: 아이디 · 비밀번호 · 비밀번호 확인 · **이메일 또는 전화번호**(하나는 필수)
- 아이디 · 이메일 · 전화번호는 **중복 불가**
- 탈퇴는 **소프트 삭제** — 진도는 지우고 계정 줄은 남깁니다 (재가입 방지)
- 아이디 찾기 있음. **비밀번호 찾기는 없습니다** (아래 참고)

---

## 이 프로젝트만의 중요한 구조

### 아이디는 내부적으로 가짜 주소가 됩니다

Supabase 는 원래 이메일로 로그인합니다. 아이디를 쓰려고 이렇게 바꿔 넣습니다.

```
hong1234  →  hong1234@hsk3.local
```

**화면에는 절대 안 나옵니다.** 사용자는 아이디만 봅니다.
덤으로 아이디 중복이 Supabase 차원에서도 막힙니다.

이 규칙은 `web/lib/useAuth.ts` 의 `loginEmail()` 과
`supabase/migrations/20260804080000_username.sql` 이 짝입니다. **한쪽만 고치면 안 됩니다.**

### 확인 메일을 안 보냅니다

`hong1234@hsk3.local` 로는 메일을 받을 수 없습니다.
그래서 마이그레이션 9가 계정이 만들어지는 순간 "확인 끝난 계정" 으로 표시합니다.
**Supabase 대시보드에서 Confirm email 을 켜도 이 트리거가 이깁니다.**

### 비밀번호를 잊으면 관리자가 바꿔줘야 합니다

재설정 메일을 보낼 곳이 없습니다. 가입 화면에도 그렇게 적어뒀습니다.
문의가 오면 Supabase SQL Editor 에서:

```sql
update auth.users
   set encrypted_password = extensions.crypt('새비밀번호', extensions.gen_salt('bf'))
 where email = 'hong1234@hsk3.local';
```

### 탈퇴한 계정을 되살리려면

```sql
update public.profiles set is_active = true, deleted_at = null
where username = 'hong123';
```

진도는 이미 지워졌으니 처음부터 시작합니다. 아이디만 되살아납니다.

### 못 보낸 진도는 브라우저에 쌓입니다

지하철에서 신호가 끊기면 `web/lib/pending.ts` 가 받아둡니다.
연결되면 알아서 보냅니다. 화면에는 "N개를 아직 못 보냈어요" 가 뜹니다.

---

## 남은 일 (순서대로)

### 1. 학습 품질 — 이게 제일 급합니다

매일 쓰는 화면이라 체감이 가장 큽니다.

| | 무엇이 잘못됐나 | 어디 |
|---|---|---|
| **가** | **"틀리면 3분 뒤" 가 한 번도 동작 안 함.** `daily_words` 가 `status <> 'known'` 을 먼저 보기 때문에, 틀린 단어는 `due_at` 과 상관없이 즉시 다시 나옵니다. 게다가 `wrong_count desc` 정렬이라 맨 앞에 옵니다 | `20260804060000_review.sql:145` |
| **나** | **오답 보기에 같은 답이 두 개 뜹니다.** 오답끼리 비교를 안 합니다. 뜻이 글자까지 같은 쌍 6개(`开展`/`展开`, `情况`/`状况` 등), 부분적으로 겹치는 쌍 112개, 한자까지 같은 `调`/`调` | `web/lib/quiz.ts:94` |
| **다** | 단어 상세의 "외웠어요" 를 네 번 연타하면 복습이 35일 뒤로 밀립니다. 퀴즈 정답과 같은 무게로 기록돼서요 | `WordDetail.tsx:36` |
| **라** | **폰에서 품사가 숨겨져 `把`/`背` 가 똑같은 줄 두 개로 보입니다.** `02-plan.md` 가 경고했던 바로 그 상황 | `web/components/ui.tsx:78` |

### 2. 나머지 중요한 것

- 가입이 실패해도 화면에 아무 표시가 없을 수 있음 (`useAuth.ts` 의 `signUp`)
- 비밀번호 변경 화면 없음 (`supabase.auth.updateUser()` 로 가능)
- 연락처 수정 화면 없음 — **서버는 이미 만들어뒀습니다** (`update_contact` 함수)
- 진도를 보는 방법이 막대 하나뿐. `seen_count`·`correct_count`·`streak` 를 다 저장하는데 안 보여줌

### 3. 예문 검수 973개

한자·병음·품사는 공식 목록 그대로라 믿을 수 있지만,
**한국어 뜻과 예문은 새로 만든 것입니다.** 사람이 아직 안 봤습니다.

```sql
update public.words set verified = true, verified_at = now()
where id in ('L3-0001', 'L3-0002');
```

시드를 다시 돌려도 이 표시는 지워지지 않습니다. 일부러 그렇게 만들었어요.

### 4. 배포 (마지막)

Vercel에 올립니다. **루트 디렉터리를 `web` 으로 지정**하고 환경 변수 2개를 넣습니다.

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
```

`SUPABASE_DB_URL` 은 **절대 넣지 마세요.** 사이트에 필요 없고, 넣으면 위험합니다.

배포 뒤 반드시 **폰으로 열어서** 확인하세요. 밝은 화면·어두운 화면 둘 다요.

---

## 다시 밟으면 안 되는 지뢰

전부 실제로 한 번씩 겪은 것들입니다.

| | 어떻게 되나 | 어떻게 피하나 |
|---|---|---|
| `progress` 를 `insert` 로 저장 | 두 번째 클릭에 `409` | `mark_word` 함수를 씀 |
| `v_words` 를 그냥 `drop` | `cannot drop view ... depend on it` | `drop view ... cascade` |
| 마이그레이션을 한 번만 돌려보고 넘어감 | **두 번째 실행에서만** 터짐 | 고친 뒤 반드시 두 번 연속 실행 |
| Tailwind `@theme` 을 미디어 쿼리 안에 | 낮에도 밤에도 어두운 화면 | `@media` 안에서는 `:root` 로 변수만 다시 정의 |
| 고쳤는데 화면이 그대로 | 옛 서버가 예전 빌드를 주고 있음 | 서버를 껐다 켠다 |
| 사이트를 폴더 맨 위에 만들기 | `create-next-app` 이 거부 | 사이트는 `web/` 안에 |
| CSV 값 안에 반각 쉼표 | 칸이 밀림 | 중국어 문장부호(`，`)는 괜찮고 `,` 만 피함 |
| `204` 를 성공으로 읽기 | 권한에 막혀도 성공처럼 보임 | 바꾼 뒤 **다시 읽어서** 확인 |
| **useEffect 신호에 `statusOf` 넣기** | 답을 저장할 때마다 카드가 다시 만들어져 **1번 문제로 되돌아감** | ref 에 담아 쓰기 (`study/page.tsx:35`) |
| **RLS 로 칸을 막으려 함** | RLS 는 줄 단위만 막습니다. 탈퇴한 사람이 스스로 되살아났습니다 | 칸 단위는 `grant update (칸이름)` |
| **에러를 안 보고 `data` 만 씀** | 신호가 끊기면 "없다" 로 읽혀서, 아이디가 빈칸이 되고 확인 장치가 뚫림 | `error` 를 먼저 확인 |
| **저장될 때까지 다음으로 안 넘김** | 지하철에서 1번 문제에 갇힘 | 못 보낸 것을 적어두고 넘어가기 |
| `example.com` 으로 가입 시험 | Supabase 가 거부 | 실제 도메인 모양을 쓰기 |

---

## 자주 쓰는 명령

폴더 맨 위(`c:\workspace\hsk3-prep`)에서 실행합니다.

| 명령 | 뭘 |
|---|---|
| `npm run db:check` | 지금 몇 개인지 · 한자 안 깨졌는지 · 권한 · 함수 |
| `npm run db:seed` | `data/` 의 CSV를 DB에 반영 (몇 번을 돌려도 같음) |
| `npm run db:push` | 스키마 변경 반영 |
| `npm run dev` | 사이트 켜기 → `localhost:3000` (고칠 때) |
| `npm run build` + `npm --prefix web run start` | 진짜 사이트처럼 보기 (개발 로고 없음) |
| `npm run env:sync` | 공개 키만 `web/.env.local` 로 옮기기 |

---

## 기록은 계속 남겨주세요

이 프로젝트의 진짜 결과물은 사이트가 아니라 [05-prompt-log.md](05-prompt-log.md) 입니다.

`/clear` 를 해도 그 파일은 남습니다. **새 대화에서 막히는 게 생기면 거기에 이어서 적으면 됩니다.**

2회차 기록은 아직 안 적었습니다. 특히 이 세 가지가 시즌 2 문서에 넣을 만합니다.

1. **브라우저로 직접 돌려보지 않았으면 못 찾았을 버그** — 빌드도 통과하고 타입도 맞는데, 실제로는 1번 문제에서 안 넘어갔습니다
2. **AI에게 "팀으로 검토시키기"** — 보안·코드·사용자 경험 셋으로 나눠 훑게 했더니 치명적인 것만 6개 나왔습니다
3. **막힌 것을 솔직히 말하기** — 메일 인증 때문에 가입 시험을 못 하겠다고 하고 멈췄다가, 대시보드를 안 건드리는 방법을 찾았습니다
