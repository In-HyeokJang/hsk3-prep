# 새 대화에서 이어가기

> **`/clear` 를 하면 지금까지 나눈 대화가 사라집니다.**
> 이 파일은 그때 잃어버리면 안 되는 것만 적어둔 곳입니다.
>
> 마지막 갱신: 2026-08-08 (5회차 작업 끝) · 단어 973/973 · 검수 0

---

## 지금 어디까지 왔나

| | 상태 |
|---|---|
| 자료 | 공식 3급 973단어 · 한국어 뜻과 예문 973개 전부 · **기계 검사 통과(고칠 것 0건)** |
| 데이터베이스 | 표 6개 · 보기 2개 · **마이그레이션 22개** · 권한 설정 완료 |
| 사이트 | **화면 6개** + 로그인 화면 · 반응형 · PWA · 밝은/어두운 화면 |
| 학습 | **5가지 문제** (뜻 쓰기 / 뜻 고르기 / 한자 고르기 / 빈칸 / 병음) + 간격 반복 |
| 성조 | 눈으로 고르기 + **🎤 말해보기 (마이크로 높낮이 재기)** |
| 소리 | 읽어주기(듣기) · 소리내어 읽기(받아 적어 견주기) |
| 계정 | 아이디 회원가입 · 로그인 · 탈퇴 · 아이디 찾기 · **비밀번호 변경** |
| 깃허브 | https://github.com/In-HyeokJang/hsk3-prep · `dev` 에 **올려둠(push)** |
| 배포 | **아직** ← 지금 할 것 |
| 검수 | **아직 0개** (기계 몫은 끝, 사람 몫 남음) |

---

## 새 대화를 열면 이 말부터

```
c:\workspace\hsk3-prep 프로젝트를 이어서 할 거야.
docs/09-handoff.md 와 docs/10-wbs.md 를 읽고, 지금 상태를 확인해줘.
```

---

## ▶ 지금 할 것 — 배포

**WBS 의 코딩 항목은 다 끝났습니다.** 남은 것은 두 가지고, 둘 다 사장님 손이 필요합니다.

### 1. 폰에서 눈으로 봐야 하는 것 (10분)

아래 셋은 **기기마다 되고 안 되고가 갈려서** 제가 확인할 수 없었습니다.
`npm run dev` 로 켜고 폰에서 열어보셔야 합니다.

| 어디 | 뭘 보나 |
|---|---|
| `/words/L3-0001` 의 스피커 | 소리가 나나. 안 나면 **버튼이 아예 안 보여야** 정상입니다 |
| `/tone` → **🎤 말해보기** | "아—" 3초로 기준을 잡고, 1·2·3·4성을 내면 맞게 잡나 |
| `/words/L3-0001` 의 **소리내어 읽기** | 예문을 읽으면 받아 적나 (크롬 계열 + 인터넷 필요) |

### 2. Vercel 에 올리기

**루트 디렉터리를 `web` 으로 지정**하고 환경 변수 2개를 넣습니다.

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
```

`SUPABASE_DB_URL` 은 **절대 넣지 마세요.** 사이트에 필요 없고, 넣으면 위험합니다.

배포 뒤 반드시 **폰으로 열어서** 확인하세요. 밝은 화면·어두운 화면 둘 다요.

---

## 5회차(2026-08-08)에 바뀐 것

WBS 4번부터 15번까지 한 번에 진행했습니다. **화면이 두 개 늘었습니다** (`/review`, 성조의 말해보기).

### 새로 생긴 것

| | |
|---|---|
| **병음 고르기** | 한자를 보여주고 병음 4개 중 고르기 (10문제 중 3개) |
| **오답 노트 `/review`** | 많이 틀린 순 20개 + "약한 것만 10개 풀기" |
| **비밀번호 변경** | 홈 설정에서. **지금 비밀번호를 먼저 확인**합니다 |
| **오류 신고** | 단어 상세 맨 아래. 같은 단어는 하루 한 번 |
| **진도 화면** | 오늘 푼 수·맞힌 비율·**며칠 연속**·복습할 것·상태별·약한 단어 |
| **읽어주기 🔊** | 한자·예문 옆 스피커 |
| **성조 말해보기 🎤** | 마이크로 목소리 높낮이를 재서 몇 성인지 판정 + 곡선 그림 |
| **소리내어 읽기** | 예문을 읽으면 받아 적어 **놓친 글자에 색** |
| **주제별 풀기** | 단어장에 주제 19개 |
| **한자 가족** | `学习` → `学校·同学·学生`, 겹치는 글자에 색 |
| **틀린 것만 바로 다시** | 결과 화면에서 3분 안 기다리고 |
| **학습량 고르기** | 5 / 10 / 20 |
| **즐겨찾기 별표** | 단어장 거르개 + "즐겨찾기만 풀기" |
| **쓰기를 학습 안으로** | 채점 결과에서 바로 써보기 |
| **`npm run db:audit`** | 자료 내용 검사 11가지 |

### 만들면서 실제로 터진 것 (전부 고쳤습니다)

이번 회차에서 **직접 겪은** 것들입니다. 다시 밟으면 안 됩니다.

| | 무슨 일이 있었나 |
|---|---|
| **뜻이 같은 보기** | `把`(자루·손잡이) 문제에 `支`(자루·가늘고 긴 것)가 보기로. **정답이 둘.** 괄호 안 설명이 달라서 통과했습니다. 괄호를 지우고 견주게 고쳐서 **68쌍 → 0쌍** |
| **가입이 조용히 실패** | 같은 아이디로 다시 가입하면 Supabase 가 **에러 없이** 가짜 사용자(`identities: 0`)를 돌려줍니다. 화면에 아무 말도 안 나왔습니다 |
| **음높이 옥타브 오류** | 220Hz 를 **73Hz(3분의 1)** 로 읽었습니다. 배음 때문에 두세 주기를 밀어도 잘 겹칩니다. "제일 잘 겹치는 곳" 이 아니라 **"거의 같은 높이의 첫 봉우리"** 를 고르게 고쳤습니다 |
| **`为 · 为` 가 나란히** | 한자 가족에서. 공식 목록에 `为`(동사)/`为`(개사)가 따로 있어서요. 한자 기준 하나만 남깁니다 |
| **별표가 일정을 밀어냄** | 안 본 단어에 별표만 눌러도 그 단어가 **오늘의 1번**으로 튀어나왔습니다. `due_at` 이 빈 줄을 "제일 오래 밀림" 으로 봤기 때문입니다. **"아직 일정 없음"** 으로 고쳤습니다 |

---

## 이 프로젝트만의 중요한 구조

### 아이디는 내부적으로 가짜 주소가 됩니다

```
hong1234  →  hong1234@hsk3.local
```

**화면에는 절대 안 나옵니다.** `web/lib/useAuth.ts` 의 `loginEmail()` 과
`supabase/migrations/20260804080000_username.sql` 이 짝입니다. **한쪽만 고치면 안 됩니다.**

### 확인 메일을 안 보냅니다

`hong1234@hsk3.local` 로는 메일을 받을 수 없습니다.
마이그레이션 9가 계정이 만들어지는 순간 "확인 끝난 계정" 으로 표시합니다.
**Supabase 대시보드에서 Confirm email 을 켜도 이 트리거가 이깁니다.**

### 비밀번호를 잊으면

이제 **스스로 바꾸는 화면은 있습니다** (홈 → 설정). 하지만 **잊었을 때 되찾는 길은 없습니다.**
재설정 메일을 보낼 곳이 없어서요. 문의가 오면 Supabase SQL Editor 에서:

```sql
update auth.users
   set encrypted_password = extensions.crypt('새비밀번호', extensions.gen_salt('bf'))
 where email = 'hong1234@hsk3.local';
```

### 탈퇴한 계정을 되살리려면

```sql
update public.profiles set is_active = true, deleted_at = null
where username = 'hong123';
```

진도는 이미 지워졌으니 처음부터 시작합니다. 아이디만 되살아납니다.

### 신고 들어온 것 보기

```sql
select * from public.reports where not handled order by created_at desc;
update public.reports set handled = true, handled_at = now() where word_id = 'L3-0001';
update public.words   set verified = true, verified_at = now() where id = 'L3-0001';
```

`npm run db:audit` 이 이것도 같이 보여줍니다.

### 못 보낸 진도는 브라우저에 쌓입니다

지하철에서 신호가 끊기면 `web/lib/pending.ts` 가 받아둡니다.
연결되면 알아서 보냅니다. 화면에는 "N개를 아직 못 보냈어요" 가 뜹니다.

**별표는 여기 안 들어갑니다.** 다시 누르면 그만이고, 나중에 몰아 보내면
순서가 꼬여 껐던 별표가 되살아나는 쪽이 더 나쁩니다.

---

## 함수의 최종 정의가 어디 있나

마이그레이션은 **매번 전부 다시 실행**됩니다. 그래서 같은 함수가 여러 파일에 나오면
**가장 나중 파일이 이깁니다.** 고칠 때는 여기를 봐야 합니다.

| 함수 | 최종 정의 |
|---|---|
| `mark_word` | 16 `20260805020000_manual_mark.sql` |
| `log_attempt` | 17 `20260807010000_log_attempt.sql` |
| `report_word` · `withdraw_account` | 19 `20260807030000_reports.sql` |
| `star_word` · `daily_words` · `my_stats` | **21 `20260808010000_star.sql`** |
| `attempts` 유형 목록 | 22 `20260808020000_write_quiz.sql` |

---

## 다시 밟으면 안 되는 지뢰

전부 실제로 한 번씩 겪은 것들입니다.

| | 어떻게 되나 | 어떻게 피하나 |
|---|---|---|
| `progress` 를 `insert` 로 저장 | 두 번째 클릭에 `409` | `mark_word` 함수를 씀 |
| `v_words` 를 그냥 `drop` | `cannot drop view ... depend on it` | `drop view ... cascade` |
| 마이그레이션을 한 번만 돌려보고 넘어감 | **두 번째 실행에서만** 터짐 | 고친 뒤 반드시 두 번 연속 실행 |
| Tailwind `@theme` 을 미디어 쿼리 안에 | 낮에도 밤에도 어두운 화면 | `@media` 안에서는 `:root` 로 변수만 다시 정의 |
| 고쳤는데 화면이 그대로 | 옛 서버가 예전 빌드를 주고 있음 | 서버를 껐다 켠다 |
| CSV 값 안에 반각 쉼표 | 칸이 밀림 | 중국어 문장부호(`，`)는 괜찮고 `,` 만 피함 |
| `204` 를 성공으로 읽기 | 권한에 막혀도 성공처럼 보임 | 바꾼 뒤 **다시 읽어서** 확인 |
| **useEffect 신호에 `statusOf`·`progress`·학습량 넣기** | 답을 저장할 때마다 카드가 다시 만들어져 **1번 문제로 되돌아감** | ref 에 담아 쓰기 |
| **RLS 로 칸을 막으려 함** | RLS 는 줄 단위만 막습니다 | 칸 단위는 `grant update (칸이름)` |
| **에러를 안 보고 `data` 만 씀** | 신호가 끊기면 "없다" 로 읽혀서 확인 장치가 뚫림 | `error` 를 먼저 확인 |
| **저장될 때까지 다음으로 안 넘김** | 지하철에서 1번 문제에 갇힘 | 못 보낸 것을 적어두고 넘어가기 |
| `example.com` 으로 가입 시험 | Supabase 가 거부 | 실제 도메인 모양을 쓰기 |
| **`status` 로 먼저 거르기** | 애써 정한 `due_at` 이 통째로 무시됨 | 복습은 **시각 하나만** 보고 정하기 |
| **오답을 정답하고만 비교** | 보기 네 개 중 둘이 같은 말 | 이미 고른 오답과도 견주기 |
| **괄호까지 통째로 견주기** | `자루(손잡이)` / `자루(가늘고 긴 것)` 이 서로 다른 뜻으로 통과 | 괄호를 지우고 견주기 |
| **버튼 표시를 퀴즈 정답으로 기록** | 연타 네 번에 복습이 35일 뒤로 | 손으로 누른 것은 따로 취급 |
| **중요한 것을 폰 화면에서 숨기기** | 서로 다른 단어가 같은 줄로 보임 | `hidden sm:inline` 전에 "폰에서 없어도 되나" 묻기 |
| **에러가 없으면 성공으로 읽기** | 가입이 조용히 실패해도 화면에 아무 말이 없음 | 돌려받은 것을 뜯어보기 |
| **자기상관에서 제일 큰 봉우리 고르기** | 220Hz 를 73Hz 로 읽음 | 거의 같은 높이의 **첫** 봉우리 |
| **`due_at` 이 빈 줄을 "제일 오래 밀림" 으로 보기** | 별표만 눌러도 오늘의 1번으로 튀어나옴 | 빈 것은 "아직 일정 없음" |
| **저장소(localStorage) 값을 믿기** | 손으로 고친 `9999` 가 서버로 넘어감 | 목록에 없는 값은 기본값으로 |
| **늘 "없음" 만 말하는 검사** | 검사가 있는데 아무것도 안 잡힘 | 일부러 틀린 줄을 넣어 잡히는지 보기 |

---

## 자주 쓰는 명령

폴더 맨 위(`c:\workspace\hsk3-prep`)에서 실행합니다.

| 명령 | 뭘 |
|---|---|
| `npm run db:check` | **제대로 들어갔나** — 줄 수 · 한자 안 깨졌는지 · 권한 · 함수 |
| `npm run db:audit` | **내용이 맞는가** — 번체자 · 성조 없는 병음 · 빈 예문 · 신고 |
| `npm run db:seed` | `data/` 의 CSV를 DB에 반영 (몇 번을 돌려도 같음) |
| `npm run db:push` | 스키마 변경 반영 |
| `npm run dev` | 사이트 켜기 → `localhost:3000` |
| `npm run build` | 빌드 확인 |
| `npm run env:sync` | 공개 키만 `web/.env.local` 로 옮기기 |

---

## 기록은 계속 남겨주세요

이 프로젝트의 진짜 결과물은 사이트가 아니라 [05-prompt-log.md](05-prompt-log.md) 입니다.

`/clear` 를 해도 그 파일은 남습니다. 5회차에서 시즌 2 문서에 넣을 만한 것:

1. **"확인할 수 없는 것은 확인할 수 있게 쪼갠다"** — 마이크는 못 써봤지만, 높낮이를 재는
   규칙만 따로 떼어 가짜 소리로 시험했더니 **옥타브 버그**가 나왔습니다. 화면과 붙여뒀으면
   못 찾았을 것입니다
2. **"검사가 정말 잡는지도 검사한다"** — `db:audit` 이 973개에서 0건이라고 했는데,
   그게 "깨끗하다" 인지 "검사가 고장났다" 인지 알 수 없었습니다. 일부러 틀린 줄을 넣어봤습니다
3. **기능 하나가 남의 규칙을 조용히 깬다** — 별표는 그냥 표시일 뿐인데,
   진도 줄이 생기면서 **복습 순서**를 밀어냈습니다. 붙일 때마다 "이게 뭘 건드리나" 를 봐야 합니다
