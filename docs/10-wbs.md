# 앞으로 할 일 (WBS)

> 이 파일은 **작업 목록**입니다. `/clear` 를 해도 남습니다.
> 하나 끝낼 때마다 `[ ]` 를 `[x]` 로 바꾸고, 아래 **기록** 칸에 커밋 번호를 적습니다.
>
> 만든 날: 2026-08-07 (4회차) · **이 목록은 14·15번만 남기고 끝났습니다**
>
> **⚠ 지금 하는 일은 이 파일이 아니라 [11-wbs2.md](11-wbs2.md) 에 있습니다.**
> 6회차(2026-08-10)부터는 그쪽입니다. `/hsk3-next` 를 치면 알아서 이어갑니다.

---

## 어떻게 진행하나

- **위에서부터 하나씩.** 순서에는 이유가 있습니다 (아래 "왜 이 순서인가" 참고)
- 한 번에 하나만. 끝나면 눈으로 확인하고, 커밋하고, 다음으로
- 순서를 바꾸고 싶으면 바꿔도 됩니다. 다만 **1번만은 먼저** 해야 합니다

---

## 0단계 · 기반 (제일 먼저)

### [x] 1. 푼 기록 남기기 시작 (`attempts`) — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 문제를 풀 때마다 조용히 기록을 남깁니다. 화면은 하나도 안 바뀝니다 |
| **왜 급한가** | 표는 만들어져 있는데 **0줄**입니다. 지금 푸는 문제들이 기록 없이 사라지고 있고, **지나간 건 나중에 되살릴 수 없습니다** |
| **뭘 기록** | 누가 · 어떤 단어를 · 어떤 문제 유형으로 · 맞았나 틀렸나 · **몇 초 만에 답했나** · (틀렸다면) 뭘 골랐나 |
| **어디** | `web/app/study/page.tsx` 의 답 저장하는 자리, `web/lib/api.ts` |
| **주의** | 답한 시간(`answered_ms`)을 처음부터 넣습니다. 나중에 추가하면 그 전 기록이 전부 빕니다 |
| **난이도** | 보통 |
| **기록** | 마이그레이션 17 `log_attempt` · `api.ts` · `study/page.tsx`. 실제로 4줄 쌓이는 것 확인. 기록이 실패해도 문제 풀이는 안 멈추게 했고, '다음'이 아니라 **답하는 순간**에 남깁니다 |

---

## 1단계 · 문제 유형 늘리기

> **지금 3가지 문제가 전부 "뜻"만 묻습니다.** 뜻은 아는데 못 읽는 단어가 쌓여도 사이트가 못 알아챕니다.

### [x] 2. 빈칸 채우기 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 예문에서 그 단어만 가리고 맞히게 합니다 — `请___这个按钮。` |
| **왜** | 예문 973개가 지금 **정답 화면 장식**으로만 쓰입니다. 새 자료를 한 줄도 안 만들고 문제 유형이 하나 늘어나는 유일한 방법 |
| **어디** | `web/lib/quiz.ts` (규칙), `web/app/study/page.tsx` (화면) |
| **주의** | 단어가 예문 안에 안 들어 있는 줄은 이 유형에서 빼야 합니다 |
| **난이도** | 보통 |
| **기록** | `quiz.ts` 에 `canBlank`·`blankSentence`, `study/page.tsx` 에 화면. 세 문제에 한 번씩 나옵니다. 병음·뜻·품사는 감췄습니다(전부 답을 알려줌). 967/973이 대상이고 못 내는 6개는 접사(`…极了` 등)라 예문에 그 글자가 없습니다. **9,670문제를 만들어 확인** — 보기 겹침 0, 오답이 예문에 그대로 보이는 경우 0 |

### [x] 3. 성조 맞히기 (눈으로) — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 한자를 보여주고 **1성 · 2성 · 3성 · 4성 · 경성** 중 고르게 합니다 |
| **왜** | 한국 사람이 제일 많이 놓치는 지점인데, 지금 사이트에 성조를 확인하는 자리가 아예 없습니다. 버튼 5개라 폰에서 한 손으로 됩니다 |
| **어디** | `web/lib/quiz.ts`, 병음에서 성조를 뽑아내는 코드 (`api.ts` 의 글자표를 거꾸로 읽으면 됨) |
| **난이도** | 쉬움 |
| **기록** | 커밋 `af47127` · **메뉴에 '성조' 를 새로 만들었습니다** (`web/app/tone/page.tsx`). 처음에는 학습(/study) 안에 섞었는데, 뜻을 떠올리다 갑자기 높낮이를 물으면 머리를 다른 데로 돌려야 해서 빼냈습니다. 한 번에 20문제를 몰아서 풀고, 맞히면 저절로 넘어가고 틀렸을 때만 멈춥니다. 끝나면 놓친 것만 모아 보여줍니다.<br>**진도(`progress`)는 건드리지 않습니다** — 성조를 틀렸다고 뜻 복습 날짜가 당겨지면 안 됩니다. 푼 기록(`attempts`)만 남깁니다.<br>성조는 병음의 성조 기호를 세어서 한자와 순서대로 짝지었습니다. 마이그레이션 18 (`tone` 유형 허용). **973단어로 다 돌려 확인** — 성조 어긋남 0, 학습에 성조 안 섞임, 20문제 뽑기에 중복·잘못된 문제 0, 화면에 보이는 게 같은데 답이 갈리는 단어 0. 낼 수 있는 단어 961개이고 못 내는 12개는 접사(`…极了`)·儿화(`空儿`)·가운데가 경성인 것(`能不能`)이라 일부러 뺐습니다 |

### [x] 4. 병음 고르기 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 한자를 보여주고 병음 4개 중 고르게 합니다 |
| **왜** | "뜻은 아는데 못 읽는" 상태를 잡아줍니다. 듣기 시험의 바닥 실력 |
| **⚠ 주의** | **오답을 고를 때 병음까지 비교해야 합니다.** `把/把` 는 병음이 같아서 안 그러면 **정답이 두 개**가 됩니다. 3회차에 67군데 터졌던 바로 그 사고 (`quiz.ts` 의 `clashes()`) |
| **⚠ 여기가 틀렸었습니다** | 원래 이 칸에 *"`把/把`, `背/背` 는 병음이 같아서"* 라고 적혀 있었는데 **`背` 는 병음이 다릅니다**(bēi 업다 / bèi 등). 그래서 "병음이 같으면 제외" 라는 방어가 `背` 를 못 걸렀고, **背 문제의 보기에 `bēi` 와 `bèi` 가 같이 뜨는 사고**가 났습니다. 둘 다 옳은 읽기라 정답이 둘입니다. 6회차 T11 에서 고쳤습니다 — 한자가 겹치는 여섯 쌍 중 **셋이 병음이 다릅니다**(背 · 调 diào/tiáo · 精神 jīngshén/jīngshen) |
| **난이도** | 쉬움 |
| **기록** | `quiz.ts` 에 `canPinyin`·`pickWrongPinyin`, `study/page.tsx` 에 화면. 마이그레이션은 필요 없었습니다 — `attempts` 가 이미 `pinyin` 을 받고 있었습니다.<br>**오답을 뜻이 아니라 병음으로 견줍니다.** 뜻이 전혀 달라도 읽는 소리가 같으면 보기 두 개가 똑같은 글자로 보입니다 (是/事 shì · 在/再 zài · 他/她 tā). 정답과도, 이미 고른 오답끼리도 견줍니다. 글자 수가 같은 단어를 먼저 써서 길이만 보고 찍는 것도 막았습니다.<br>설정에서 '병음 보이기' 를 켰어도 이 문제에서만은 감춥니다. 10문제 중 3개(2·6·10번째)로 나옵니다.<br>**973단어로 9,730문제를 만들어 확인** — 보기 병음 겹침 0 · 정답 빠짐 0 · 같은 한자 보기 0 |

### [x] 4-1. 뜻이 사실상 같은 보기 걸러내기 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 4지선다에 **뜻이 같은 보기**가 같이 나오는 걸 막습니다 |
| **어떻게 찾았나** | 1번(기록)을 켠 첫날 바로 잡혔습니다 — 把(자루·손잡이 있는 것) 문제에 **支(자루·가늘고 긴 것)** 가 보기로 나왔습니다. 둘 다 한국어로 "자루" 라서 **정답이 둘**입니다 |
| **왜 3회차 수정이 못 잡았나** | `clashes()` 가 뜻 글자를 비교하는데, 괄호 안 설명이 달라서 서로 다른 뜻으로 통과했습니다 |
| **어디** | `web/lib/quiz.ts` 의 `clashes()` — **괄호 앞부분만 떼어서** 견주면 잡힙니다 |
| **확인** | 973단어로 문제를 다 만들어서 겹치는 보기가 몇 개인지 세어봅니다 (3회차에 한 방식 그대로) |
| **난이도** | 쉬움 |
| **기록** | `quiz.ts` 에 `core()` 를 넣어 **괄호 묶음을 지우고** 견줍니다. 앞을 자르지 않고 괄호만 지웁니다 — `(나쁜) 결과` 처럼 괄호가 앞에 오는 뜻이 있어서요.<br>**973단어로 15,530문제를 만들어 확인 — 68쌍 → 0쌍.** 사장님이 잡으신 把(자루·손잡이)/支(자루·가늘고 긴 것) 말고 **被(~에 의해)/由(~이·~에 의해)** 도 같이 잡혔습니다.<br>병음 고르기(4번)는 이 셈에서 뺐습니다. 거기는 보기에 병음이 뜨므로 뜻이 겹쳐도 답이 갈리지 않습니다 |

### [x] 5. 오답 노트 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 많이 틀린 단어만 모아 보고, "이것만 10개 풀기" |
| **왜** | `wrong_count` 를 열심히 저장하면서 화면에 한 번도 안 보여줍니다 |
| **덤** | 나중에 "AI 분석"(14번)을 얹을 화면이 바로 여기입니다 |
| **난이도** | 쉬움 |
| **기록** | **메뉴에 '오답' 을 새로 만들었습니다** (`web/app/review/page.tsx`). 많이 틀린 순 20개까지 보여주고, 맨 위 버튼으로 `/study?only=wrong` 에 들어가 **그 단어들만 10개** 풉니다.<br>고르는 규칙은 `web/lib/weak.ts` 한 곳에 뒀습니다 — 목록과 실제로 나오는 문제가 다르면 안 되니까요. 틀린 횟수가 같으면 맞힌 비율이 낮은 것을 앞에 둡니다.<br>**서버를 새로 부르지 않습니다.** `progress` 를 이미 받아둬서 마이그레이션도 새 함수도 없습니다.<br>주소의 `?only=wrong` 을 읽으려고 `/study` 를 `Suspense` 로 감쌌습니다 (Next 규칙). 진도(`progress`)는 **ref 에 담아** 씁니다 — 신호로 쓰면 답할 때마다 묶음이 새로 만들어져 1번 문제로 되돌아갑니다.<br>실제 진도로 확인 — 6줄이 `了`(4번·20%) → `把`·`被`·`为`(2번) 순으로 나옵니다 |

---

## 2단계 · 계정과 신뢰

### [x] 6. 비밀번호 변경 화면 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 로그인한 사람이 스스로 비밀번호를 바꿉니다 |
| **왜** | 지금은 잊으면 **관리자가 SQL로 직접 바꿔줘야** 합니다 (`09-handoff.md`). 남에게 쓰게 하려면 필수 |
| **어디** | `web/lib/useAuth.ts` (`supabase.auth.updateUser()`), 홈 설정 칸 |
| **난이도** | 쉬움 |
| **기록** | `useAuth.ts` 에 `changePassword`·`checkNewPassword`, `components/ChangePassword.tsx` 에 화면. 홈 **설정** 칸에 접어서 뒀습니다.<br>**★ 지금 비밀번호를 먼저 확인합니다.** Supabase 는 로그인만 돼 있으면 그냥 바꿔줍니다 — 폰을 잠깐 놓아둔 사이에 남이 비밀번호를 바꿔 계정을 가져갈 수 있습니다. 그 비밀번호로 다시 로그인해보는 것으로 확인합니다. 아이디는 화면에서 안 받고 지금 세션에서 꺼냅니다 (내부용 주소를 사용자에게 물어볼 수 없어서요).<br>화면에 "**비밀번호 찾기가 없습니다**" 를 한 번 더 적었습니다. 가입 때만 적어두면 그때 읽고 잊습니다.<br>**시험 계정을 만들어 실제로 돌려보고 지웠습니다** — 옛 비밀번호 로그인 ✓ · 틀린 지금-비밀번호 거절 ✓ · **틀린 뒤에도 로그인 유지** ✓ · 바꾸기 ✓ · 새 것으로 로그인 ✓ · 옛 것은 막힘 ✓ · 계정 지움 ✓ |

### [x] 7. 가입 실패했을 때 화면에 표시 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 가입이 안 됐는데 아무 표시가 없을 수 있는 걸 고칩니다 |
| **왜** | 실패했는지 성공했는지 모른 채 기다리게 됩니다. 첫인상에서 바로 나가버립니다 |
| **어디** | `web/lib/useAuth.ts` 의 `signUp` |
| **난이도** | 쉬움 |
| **기록** | **에러가 없는 것과 가입된 것은 다릅니다.** `signUp` 이 이제 결과를 뜯어봅니다.<br>· **가짜 사용자**(`identities` 가 빈 배열) → "이미 쓰고 있는 아이디입니다"<br>· **user 가 아예 없음** → "가입을 처리하지 못했습니다"<br>· **계정은 생겼는데 session 이 없음** → `'need-login'` 을 돌려주고, `AuthGate` 가 **로그인 화면으로 옮기며** "가입됐습니다. 이제 ○○ 로 로그인해 주세요" 를 띄웁니다<br>좋은 소식을 띄울 자리(`notice`)가 없어서 초록 알림칸도 새로 만들었습니다. 나쁜 소식만 있으면 잘된 건지 아무 일도 안 일어난 건지 모릅니다.<br>**실제로 재현해서 확인** — 같은 아이디로 다시 가입하면 Supabase 는 **에러 없이** `identities: 0` · `session: 없음` 을 돌려줍니다. 예전 코드는 이 자리에서 아무 말도 못 했습니다. 시험 계정은 지웠습니다 |

### [x] 8. 오류·오타 신고 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | "이거 이상해요" 버튼. 눌린 것을 모아뒀다가 나중에 한 번씩 확인 |
| **왜** | **한국어 뜻과 예문 973개는 사람이 아직 안 봤습니다.** 혼자 검수하는 건 사실상 불가능한데, 쓰는 사람이 눌러주면 저절로 됩니다 |
| **필요** | 새 표 하나 (`reports`) — 누가 · 어느 단어 · 뭐가 이상한지 · 처리됐는지. 마이그레이션 1개 |
| **연결** | `words.verified` / `examples.verified` 칸이 이미 있어서 그대로 맞물립니다 |
| **주의** | 장난으로 연타하는 걸 막을 제한이 필요합니다 (같은 단어에 하루 1번 등) |
| **난이도** | 보통 |
| **기록** | **마이그레이션 19** (`reports` 표 + `report_word` 함수). 단어 상세 화면 맨 아래에 작게 "이 단어가 이상해요" (`components/ReportButton.tsx`). 종류 4가지(뜻·예문·병음/성조·그 밖) 중 하나를 고르고 한마디는 선택입니다.<br>**표에 직접 넣는 권한은 아무에게도 안 줬습니다.** 함수로만 들어옵니다 — 화면에서만 막으면 브라우저로 서버를 직접 불러 연타할 수 있습니다 (마이그레이션 12에서 겪은 일). 같은 단어는 **하루에 한 번**, 서버가 막습니다.<br>탈퇴할 때 신고도 지우도록 `withdraw_account` 를 다시 만들었습니다 — 진도는 지우면서 신고만 남으면 "다 지웠다" 가 거짓말이 됩니다. **이 마이그레이션이 `withdraw_account` 의 최종 정의입니다.**<br>`db:push` 를 **두 번 연속** 돌려 확인했고, 시험 계정으로 **아홉 가지를 실제로 확인**했습니다 — 신고 남김 ✓ · 두 번째 막힘 ✓ · 다른 단어는 됨 ✓ · 엉뚱한 종류 거절 ✓ · **표에 직접 넣기 막힘**(`permission denied`) ✓ · 내 신고만 읽힘 ✓ · 탈퇴 뒤 0줄 ✓<br>확인은 Supabase 에서 `select * from reports where not handled order by created_at desc` 로 봅니다 |

---

## 3단계 · 보여주기

### [x] 9. 진도 화면 제대로 — **2026-08-07 완료**

| | |
|---|---|
| **뭘** | 막대 하나뿐인 걸 이렇게 바꿉니다 |
| | · **오늘** — 푼 개수 · 맞힌 비율 · **며칠 연속 했는지** |
| | · **상태별** — 안 본 것 / 배우는 중 / 외운 것 (막대 하나 대신 세 칸) |
| | · **다시 볼 것** — "오늘 복습할 단어 N개" (`due_at` 이 이미 계산해두고 안 보여주는 중) |
| | · **약한 단어** — 많이 틀린 순 10개 |
| **왜** | 다 저장하면서 하나도 안 보여줍니다. 연속 일수는 계속하게 만드는 힘이 제일 큽니다 |
| **난이도** | 보통 |
| **기록** | **마이그레이션 20** (`my_stats()` 함수) + `components/StatsPanel.tsx`. 홈의 막대 하나를 네 칸으로 바꿨습니다. **연속 일수를 제일 큰 글자로** 뒀습니다.<br>**세는 일은 서버가 합니다.** '오늘' 과 '며칠 연속' 은 **한국 시각(Asia/Seoul)** 으로 못박아야 합니다 — 서버는 UTC 라서 그냥 두면 밤 9시에 푼 것이 '내일' 로 넘어가 연속 기록이 끊긴 것처럼 보입니다. 푼 기록은 계속 쌓여서 브라우저로 통째로 받아 세면 쓸수록 느려지기도 합니다.<br>**오늘 아직 안 풀었으면 어제를 기준으로 셉니다.** 아침에 열었을 때 이어온 것이 0으로 보이면 그날로 그만두게 됩니다.<br>숫자를 못 받아와도 **이 칸만 조용히 접습니다.** 오늘의 단어까지 같이 사라지면 진도 때문에 공부를 못 하게 됩니다.<br>**날짜를 지어 넣어 연속 일수 여섯 경우를 확인** — 오늘까지 사흘 → 3 · 어제까지 사흘(오늘 아직) → 3 · 어제 거름 → 1 · 이틀 전 멈춤 → 0 · 하루에 여러 번 → 하루로 · 아예 없음 → 0.<br>**시험 계정으로 실제 호출도 확인** — 갓 가입 973/0/0 · 세 문제 푼 뒤 오늘 3중 2 · 연속 1일 · 외움 2 배우는 중 1 안 본 것 970 · 세 칸 합 973 |

---

## 4단계 · 소리 🔊

> 여기서부터가 **이번에 새로 추가된 큰 덩어리**입니다.
> 셋 다 "소리"지만 **서로 완전히 다른 기술**이라 따로 갑니다. 위험도도 다릅니다.

### [x] 10. 읽어주기 (듣기) — **2026-08-07 완료** · ⚠ 소리는 아직 귀로 안 들어봄

| | |
|---|---|
| **뭘** | 한자나 예문 옆 스피커를 누르면 중국어로 읽어줍니다 |
| **왜** | 병음을 눈으로만 보면 실제 소리와 다르게 굳습니다. **듣기가 시험의 3분의 1** |
| **⚠ 위험** | **한국어 윈도우에는 중국어 목소리가 없는 경우가 많습니다.** 노트북에서 눌러도 무음. 폰은 대체로 됩니다 |
| **그래서** | "고장난 게 아니라 이 기기에 중국어 목소리가 없습니다" 안내가 **반드시** 같이 있어야 합니다. 없으면 버튼도 숨깁니다 |
| **난이도** | 어려움 (코드는 짧은데 기기마다 다름) |
| **기록** | `web/lib/speak.ts` (규칙) · `components/Speak.tsx` (버튼). 브라우저에 이미 있는 기능(SpeechSynthesis)만 씁니다 — 새 라이브러리도 소리 파일도 없습니다. 붙은 자리: **단어 상세**(한자 옆·예문 옆), **학습 채점 결과**(한자 옆·예문 옆).<br>**중국어 목소리가 없는 기기에서는 버튼이 아예 안 나옵니다.** 눌러도 소리가 안 나는 버튼은 "고장난 사이트" 로 읽힙니다. 대신 단어 상세에 **한 번만** 왜 없는지와 어떻게 넣는지를 적어둡니다 (`SpeakNote`). 한자마다 붙이면 화면이 안내문으로 뒤덮입니다.<br>목소리 목록은 **늦게 채워집니다.** 브라우저가 처음엔 빈 배열을 주고 `voiceschanged` 로 알려줍니다. 처음 값만 보면 되는 기기에서도 버튼이 안 나옵니다. 알림이 아예 안 오는 브라우저가 있어 1.2초 뒤 한 번 더 봅니다.<br>zh-CN 을 먼저 찾고 없으면 다른 중국어 목소리라도 씁니다. 조금 느리게(0.85) 읽고, 연달아 누르면 앞의 것을 끊습니다.<br>**⚠ 아직 못 한 확인:** 이 노트북에 `zh-CN`(Microsoft Huihui)이 있는 것까지는 확인했지만, **실제 소리는 사람이 눌러서 들어봐야 합니다.** 브라우저를 띄우는 도구가 없습니다. `npm run dev` → `/words/L3-0001` 에서 스피커를 눌러보세요 |

### [x] 11. 성조 발음 연습 ⭐ 사장님 아이디어 — **2026-08-07 완료** · ⚠ 마이크는 아직 안 써봄

| | |
|---|---|
| **뭘** | **마이크에 대고 소리를 내면, 1·2·3·4성이 제대로 나왔는지 판정**합니다. 목소리 높낮이를 선으로 그려서 보여줍니다 |
| **왜** | 성조는 눈으로 외워지지 않습니다. 소리를 내고 **틀렸다는 걸 즉시 알아야** 고쳐집니다. 혼자 공부할 때 제일 안 되는 게 이거고, 이 사이트에서 제일 값진 기능이 될 수 있습니다 |
| **되나?** | **됩니다.** 브라우저가 마이크 소리에서 음높이를 재고, 그 **곡선 모양**을 성조와 비교합니다 (1성 평평 / 2성 올라감 / 3성 내려갔다 올라옴 / 4성 뚝 떨어짐). 새 라이브러리 없이 만들 수 있습니다 |
| **⚠ 주의 1** | 성조는 **절대 음높이가 아니라 상대적 곡선**입니다. 사람마다 목소리 높이가 달라서, **먼저 본인 기준선을 잡는 단계**가 필요합니다 ("아—" 하고 3초) |
| **⚠ 주의 2** | 조용한 곳이어야 합니다. 마이크 권한 허용 필요. 폰은 잘 되고 노트북은 내장 마이크 품질에 따라 갈립니다 |
| **범위** | **성조만 봅니다.** "발음이 정확한가"까지는 안 봅니다 — 그건 다른 문제고, 성조만 봐도 충분히 쓸모 있습니다 |
| **어디에 붙나** | 3번에서 만든 **성조 화면(`/tone`)** 위에 얹습니다. 눈으로 고르기 옆에 "말해보기" 를 두는 모양입니다 |
| **★ 사장님이 기다리는 기능** | 3번을 확인하면서 **"1·2·3·4성을 실제로 말하면서 연습하고 싶다"** 고 하셨습니다 (2026-08-07). 순서를 앞당길지 물어보세요 |
| **난이도** | **어려움** (이 프로젝트에서 제일 어려운 작업) |
| **기록** | `/tone` 위에 **눈으로 고르기 / 🎤 말해보기** 두 갈래를 만들었습니다. 새 라이브러리 없이 브라우저 기능만 씁니다.<br>**규칙(`web/lib/pitch.ts`)과 화면(`components/ToneSpeak.tsx`)을 갈라놨습니다.** 규칙 쪽에는 마이크도 화면도 없고 숫자만 있습니다 — 그래야 마이크에 대고 백 번 말해보는 대신 **가짜 곡선으로 시험**할 수 있습니다. 실제로 그렇게 해서 아래 버그를 잡았습니다.<br>**⚠ 실제로 터진 버그 — 옥타브를 잘못 잡음.** 처음 만든 자기상관은 220Hz 를 **73Hz(3분의 1)** 로, 330Hz 를 165Hz 로 읽었습니다. 목소리에 배음이 섞여서 두 주기·세 주기를 밀어도 잘 겹치기 때문입니다. **"제일 잘 겹치는 곳" 이 아니라 "그것과 거의 같은 높이의 첫 번째 봉우리"** 를 고르도록 고쳤고, 겹침 정도를 소리 크기로 나눠 재게 했습니다. 85~440Hz 6개 전부 오차 0.1% 안으로 들어왔습니다.<br>**한 글자 단어만 냅니다** (171개 · 1성 34 · 2성 44 · 3성 38 · 4성 55). 두 글자를 주고 "뒷글자 성조를 내보세요" 하면 앞 글자를 읽을지부터 헷갈리고, 두 음절 곡선에서 뒤만 떼어내는 건 다른 차원의 일입니다. 경성도 뺐습니다.<br>기준선("아—" 3초)은 **가운뎃값**으로 잡습니다 — 앞뒤에 섞이는 헛소리가 평균은 통째로 끌고 갑니다. 마이크의 자동 음량 조절은 껐습니다(켜두면 브라우저가 목소리를 주무르며 높낮이까지 흔듭니다). 화면을 벗어나면 마이크를 반드시 끕니다.<br>**진도(`progress`)는 안 건드립니다.** 푼 기록만 남기고, 유형은 `tone` · `meta.by='voice'` 입니다 — 무엇을 봤나(성조)로 묶어야 눈으로 고른 것과 함께 볼 수 있습니다.<br>판정 결과에 **내 목소리가 그린 선**을 같이 보여줍니다. "3성입니다" 라고만 하면 뭘 고쳐야 할지 모르는데, 모양을 보면 "끝을 안 올렸구나" 가 바로 보입니다.<br>**확인:** 음높이 6/6 · 성조 판정 8/8 · **오르내리는 가짜 목소리를 만들어 조각조각 재고 판정까지 4/4** (1성 평평 · 2성 올림 · 3성 내렸다 올림 · 4성 뚝).<br>**⚠ 아직 못 한 확인: 진짜 마이크.** 브라우저를 띄우는 도구가 없습니다. `npm run dev` → `/tone` → 말해보기 에서 직접 소리를 내보셔야 합니다 |

### [x] 12. 소리내어 읽기 — **2026-08-07 완료** · ⚠ 마이크는 아직 안 써봄

| | |
|---|---|
| **뭘** | 단어나 예문을 소리내어 읽으면, 맞게 읽었는지 확인해줍니다 |
| **왜** | 11번이 "성조 하나"라면 이건 "문장 통째로". 실제로 입이 트이는 건 이 단계 |
| **⚠ 위험** | 브라우저 음성 인식(중국어)은 **기기 편차가 큽니다.** 크롬은 되고 일부 폰·브라우저는 아예 안 됩니다. 인터넷 연결도 필요 |
| **그래서** | **11번을 먼저 만들고, 잘 되는 걸 확인한 뒤에** 손댑니다 |
| **난이도** | 어려움 |
| **기록** | `web/lib/listen.ts` (규칙) · `components/ReadAloud.tsx` (화면). **단어 상세**에 붙었습니다 — 예문이 있으면 예문을, 없으면 단어를 읽습니다.<br>11번과 기술이 아예 다릅니다. 11번은 소리의 **높낮이를 직접 재고**, 이건 **글자로 받아 적어** 견줍니다. 그래서 11번 확인을 기다리지 않고 만들었습니다 (되고 안 되고가 서로에게 달려 있지 않습니다).<br>**되는 브라우저에서만 나옵니다.** 크롬 계열은 되고 파이어폭스는 아예 없습니다. 인터넷도 필요합니다.<br>**통째로 같은지만 보지 않습니다.** 한 글자 잘못 읽어도 "틀렸습니다" 만 나오면 어디를 고칠지 모릅니다. 순서를 지키며 가장 많이 겹치는 부분을 찾아(LCS) **놓친 글자에 색을 칠합니다.** 문장부호와 띄어쓰기는 안 셉니다.<br>**"틀렸다" 고 단정하지 않습니다.** 받아 적기는 소리가 비슷한 글자를 헷갈립니다 (买 mǎi / 卖 mài). 기계가 못 알아들은 것을 사람 탓으로 돌리면 안 되니, "몇 글자 맞았나" 로만 말하고 그 사실도 화면에 적어둡니다.<br>**견주는 규칙 9가지 확인** — 그대로 · 문장부호 뺌 · 띄어쓰기 뺌 · 门→们 한 글자만 · 첫 글자 빠뜨림 · 전혀 다름 · 아무 말 없음 · 긴 문장 · 买/卖 헷갈림.<br>**⚠ 아직 못 한 확인: 진짜 마이크.** 브라우저를 띄우는 도구가 없습니다 |

---

## 5단계 · 나중에 (급하지 않음)

### [x] 13. 자잘하지만 좋은 것들 — **2026-08-08 완료 (여섯 가지 전부)**

- [x] **주제별로 골라 풀기** — **2026-08-08 완료.** `/words` 에 주제 줄(19개)을 넣고, 고르면 `그 주제만 풀기` 로 `/study?topic=…` 에 들어갑니다. 주제 목록은 코드에 안 적고 **자료에서 뽑습니다**(`api.ts` 의 `topicsOf`) — 적어두면 CSV 를 고칠 때 두 곳을 맞춰야 하고, 한쪽만 고치면 눌러도 아무것도 안 나오는 주제가 생깁니다. 주제 묶음은 **안 외운 것을 먼저** 채우고 모자라면 외운 것으로 채웁니다 (고른 주제에서 "다 외우셨어요" 만 나오면 고른 뜻이 없습니다). **19개 주제 전부 확인** — 973개에 빠짐없이 붙어 있고, 거르기 어긋남 0건, 주제+검색 같이 걸림. 가족(9개)만 10개 미만입니다
- [x] **이 한자가 들어간 다른 단어** — **2026-08-08 완료.** 단어 상세의 예문 아래에 붙습니다 (`api.ts` 의 `sharingHanzi`). 자주 쓰는 것부터 8개까지, **겹치는 글자에만 색**을 칠합니다 (어느 글자가 같은지 눈에 안 들어와서요). 973개 중 921개가 묶일 단어를 가집니다.<br>**⚠ 실제로 터진 것:** `为了` 아래에 **为 · 为** 가 나란히 떴습니다 — 공식 목록에 `为`(동사)와 `为`(개사)가 따로 있어서요. 같은 글자가 두 번 보이면 고장으로 읽힙니다. **한자 기준으로 하나만** 남기게 고쳤습니다. 여기는 "이 글자가 이렇게도 쓰인다" 를 보는 자리라 글자 하나면 충분하고, 눌러 들어가면 품사까지 다 나옵니다.<br>**973단어 전부 확인** — 같은 한자 두 번 0 · 자기 자신 섞임 0 · 나와 한자가 같은 줄 섞임 0 · 겹치는 글자 없는데 나옴 0
- [x] **결과 화면에서 "틀린 것만 바로 다시"** — **2026-08-08 완료.** 학습 결과 화면 맨 위에 `틀린 N개만 바로 다시` 가 뜹니다. 틀린 게 있으면 그쪽이 진한 버튼이 되고 `10개 더 하기` 는 연한 쪽으로 물러납니다 (진한 버튼이 둘이면 어느 쪽이 먼저인지 안 보입니다).<br>**서버를 안 부릅니다.** 이번 묶음에서 틀린 단어를 화면에 담아뒀다가 그걸로 새 묶음을 만듭니다. **실제로 확인한 이유:** 틀리면 `due_at` 이 **정확히 3.0분 뒤**로 잡히고, 그 상태에서 `daily_words` 를 다시 부르면 **그 단어가 목록에서 빠집니다.** 서버에 물어서 만들면 방금 틀린 것이 안 나옵니다.<br>복습 일정(3분 뒤)은 며칠 뒤를 위한 것이고, 방금 틀린 것을 그 자리에서 한 번 더 보는 것은 다른 일입니다. 둘 다 있어야 합니다
- [x] **하루 학습량 고르기** — **2026-08-08 완료.** 홈 **설정** 칸에서 5·10·20 중 고릅니다. **오늘의 단어**와 **학습**이 같이 이 값을 씁니다.<br>설정이 하나뿐이라 `settings.ts` 가 통으로 쓰여 있었는데, 둘이 되면서 **설정 하나를 만드는 틀**(`makeSetting`)로 바꿨습니다. 담아두기·알려주기·저장하기가 똑같아서요.<br>**학습 화면에서는 ref 로 읽습니다.** 신호로 쓰면 다른 탭에서 설정을 바꾸는 순간 풀던 묶음이 새로 만들어져 1번 문제로 되돌아갑니다. 바꾼 값은 다음 묶음부터 쓰입니다. 홈은 목록이라 바로 따라갑니다.<br>**⚠ 저장된 값을 믿지 않습니다.** 이 숫자가 그대로 서버에 "몇 개 주세요" 로 넘어가는데, 브라우저 저장소는 사람이 손으로 고칠 수 있는 자리입니다. 규칙을 `toDailyCount` 로 따로 꺼내 **12가지를 확인** — `9999`·`0`·`-5`·`3`·`10.5`·`abc`·`1e3`·빈 글자·없음이 전부 기본값 10으로 돌아옵니다.<br>성조 화면(눈으로 20개 · 말해보기 8개)은 **일부러 그대로 뒀습니다.** 성조는 버튼 다섯 개로 빠르게 몰아 푸는 편이 연습이 되고, 말해보기는 한 글자에 시간이 훨씬 오래 걸립니다
- [x] **즐겨찾기 별표** — **2026-08-08 완료.** 단어 상세에 별표 버튼, 단어장에 `★ 즐겨찾기` 거르개와 `즐겨찾기만 풀기`(`/study?only=star`), 목록 줄에도 별표 표시. **마이그레이션 21** (`star_word` 함수).<br>**⚠ "복습 날짜를 건드리면 안 됨" 이 그냥 두면 지켜지지 않았습니다.** 안 본 단어에 별표를 누르면 `progress` 에 줄이 새로 생기는데, 그 줄은 `due_at` 이 비어 있습니다. 그런데 `daily_words` 가 "줄이 있고 시각이 비었으면" 을 **복습 무리에 넣고 `nulls first` 로 맨 앞**에 뒀습니다 — 별표만 눌렀는데 그 단어가 **오늘의 1번으로 튀어나옵니다.**<br>고친 방향: **`due_at` 이 비었다는 건 '아직 일정이 없다' 는 뜻**이지 '제일 오래 밀렸다' 가 아닙니다. `daily_words` 가 이제 시각 없는 줄을 **새 단어 쪽**으로 보냅니다. 이 기능이 없던 때 저장된 옛 줄도 같은 이유로 이쪽이 맞습니다. `my_stats` 도 같이 고쳤습니다 — `status='new'` 인 줄도 '안 본 것' 으로 세야 세 칸 합이 973을 지키고, '오늘 복습할 것' 에서는 시각 없는 줄을 뺐습니다(안 그러면 "복습 1개" 라고 해놓고 학습에는 안 나옵니다). **이 마이그레이션이 `star_word`·`daily_words`·`my_stats` 의 최종 정의입니다.**<br>`meta` 는 `||` 로 얹습니다. 덮어쓰면 같이 든 `streak`(연속 정답 횟수)가 사라집니다.<br>못 보낸 것을 적어두지 않습니다(진도와 다릅니다) — 별표는 다시 누르면 그만이고, 나중에 몰아 보내면 순서가 꼬여 **껐던 별표가 되살아나는** 쪽이 더 나쁩니다.<br>**시험 계정으로 확인** — 안 본 단어에 별표 → **오늘의 단어 순서 그대로** · 일정 잡힌 단어에 별표 → 날짜·상태·푼 횟수·`streak` 전부 그대로 · 껐다 켜기 · 두 번 켜도 줄 하나 · 세 칸 합 973 · '복습할 것' 에 안 세어짐
- [x] **쓰기를 학습 흐름 안으로** — **2026-08-08 완료.** 학습 화면의 **채점 결과**에 `✍️ 손으로 써보기` 를 붙였습니다. **마이그레이션 22** (`write` 유형 허용).<br>**답을 본 뒤에** 엽니다 — 답을 모르는 채로 쓰라고 하면 쓸 수가 없습니다. **눌렀을 때만** 엽니다 — 늘 열어두면 열 문제마다 열 번 획 자료를 받아옵니다. 다음 문제로 가면 닫힙니다(안 닫으면 다음 문제의 답을 보기도 전에 그 한자가 뜹니다).<br>**★ 여기서는 진도를 안 건드립니다.** 상세 페이지에서는 잘 쓰면 '외웠어요' 로 표시하지만, 학습 중에는 **뜻을 틀렸는데 글자를 잘 썼다고 외운 것이 되면 안 됩니다.** 이 문제의 진도는 이미 위에서 정해졌습니다. 푼 기록만 `write` 로 따로 남깁니다.<br>유형을 따로 둔 이유: **알아보는 것과 꺼내는 것은 실력이 다릅니다.** 고르기는 다 맞는데 쓰라면 손이 안 나가는 일이 흔한데, 같은 이름으로 묶으면 "쓰기만 유독 약하다" 를 못 뽑습니다.<br>`db:push` 두 번 연속 확인. **여덟 유형이 다 받아들여지고**, 목록에 없는 것은 거절되고, 쓰기 기록을 남겨도 진도 줄은 안 생기는 것을 확인했습니다

### [ ] 14. 틀린 곳 AI 분석 — **미룸 (2026-08-08 결정)**

1번이 기록을 충분히 쌓은 뒤에. Claude를 서버에서 불러서 "뭘 뭐랑 헷갈리는지"를 한국어로 설명.
**중국어 예문을 새로 짓게 하지 않습니다** — 설명만.

**왜 미뤘나 (2026-08-08):** 이 항목의 전제가 "1번이 기록을 충분히 쌓은 뒤" 인데,
지금 `attempts` 에 실사용 기록이 몇 줄뿐입니다. 분석할 거리가 없습니다.
Anthropic API 키(유료)와 키를 숨길 서버 쪽 가개도 새로 만들어야 합니다.
**사이트를 실제로 쓰기 시작해서 기록이 쌓인 뒤에** 하는 것이 맞습니다.

### [ ] 15. 예문 검수 973개 — **기계가 할 몫은 2026-08-08에 끝냈습니다. 사람 몫이 남았습니다**

8번(신고)이 굴러가기 시작하면 자연히 줄어듭니다.

**한 일 (2026-08-08):** `npm run db:audit` 을 만들었습니다 (`scripts/db-audit.mjs`).

`db:check` 와 다릅니다 — `db:check` 는 "제대로 들어갔나"(줄 수·한자 깨짐·권한),
`db:audit` 은 **"내용이 맞는가"** 를 봅니다. 자료를 고칠 때마다 다시 돌리면
고치다 새로 만든 어긋남도 그때 잡힙니다.

검사 11가지 — 번체자 섞임 · 성조 없는 병음 · 숫자 병음 · 반각 문장부호 ·
예문 비었음 · 예문 병음/한국어 비었음 · 예문에 그 단어 없음 · 뜻 비었음 ·
같은 예문 중복 · 예문 너무 김 · 뜻에 한자 섞임. 안 본 신고도 같이 보여줍니다.

**지금 973개 결과: 고쳐야 할 것 0건.** 번체자 0 · 성조 없는 병음 0 · 숫자 병음 0 ·
반각 부호 0 · 빈 예문 0. 사람이 볼 것 2건인데 둘 다 어법 설명이라 정상입니다
(`了` 는 `不了`, `只有` 는 `才`). 예문에 단어가 안 나오는 6건은 접사(`初（初一）`·`…极了` 등)라
검사가 알아서 뺍니다.

**⚠ 검사가 정말 잡는지도 확인했습니다.** 늘 "없음" 만 말하는 검사는 없느니만 못합니다.
일부러 틀린 줄 8개(`學習`·`ceshi`·`shu4zi4`·반각 부호·빈 예문 …)를 잠깐 넣어
**여덟 가지가 다 잡히는 것**과 문제가 있을 때 실패로 끝나는 것까지 보고 지웠습니다.

**남은 것은 사람 몫입니다.** 뜻과 예문이 맞는지는 기계가 판단할 수 없습니다.
사이트의 "이 단어가 이상해요" 로 들어온 신고부터 보시면 됩니다 (`db:audit` 이 같이 보여줍니다).

```sql
update public.words set verified = true, verified_at = now() where id in ('L3-0001');
update public.reports set handled = true, handled_at = now() where word_id = 'L3-0001';
```

---

## 왜 이 순서인가

| | |
|---|---|
| **1번이 맨 앞인 이유** | 유일하게 **늦으면 손해가 되돌릴 수 없는** 작업입니다. 나머지는 언제 해도 같습니다 |
| **2~5번이 다음인 이유** | 매일 쓰는 화면이라 효과가 제일 큽니다. 자료가 이미 다 있어서 값이 쌉니다 |
| **6~8번이 그다음인 이유** | 남에게 보여주기 전에 꼭 있어야 하는 것들입니다 |
| **소리(10~12)를 뒤로 뺀 이유** | **기기마다 되고 안 되고가 갈립니다.** 앞의 것들이 튼튼해진 뒤에 붙여야, 안 될 때 "사이트가 이상한가"로 번지지 않습니다 |

---

## 하기 전에 확인할 것

- [ ] `db/01-schema.sql` 이 **없어졌습니다.** `CLAUDE.md` 는 이 파일을 읽으라고 하는데 실제로는 없습니다.
      실제 표 정의는 `supabase/migrations/20260804010000_init_schema.sql` 에 있습니다. 둘 중 하나를 맞춰야 합니다
