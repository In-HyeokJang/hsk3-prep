# 7회차 작업 목록 (WBS 3) · 오프라인 모임 화면 `/live`

> 이 파일은 **작업 목록**입니다. `/clear` 를 해도 남습니다.
> 기획은 [13-offline-game.md](13-offline-game.md) 에 있습니다. **작업 전에 그것부터 읽습니다.**
>
> 만든 날: 2026-08-10 · 진행: **`/hsk3-next` 를 치면 여기부터 이어갑니다**

---

## 무엇을 만드나

**TV/프로젝터에 띄우고 6~10명이 다같이 보는 화면 하나.** 6회차 × 2시간.

| | |
|---|---|
| 새 화면 | **1개** (`/live`) |
| 새 표 · 라이브러리 | **0개** |
| **마이그레이션** | **1개** (관리자 칸 · L0) |
| 요금 | 무료 안 (⚠ 유료화 항목은 아래 💰 표시) |

### ★ 이 작업의 절대 규칙 3가지

1. **아무것도 저장하지 않습니다.** `mark_word` 도 `log_attempt` 도 부르지 않습니다.
   10명의 답이 사장님 개인 복습 일정에 섞이면 **되돌릴 방법이 없습니다**
   (`attempts` 는 지울 권한이 아무에게도 없습니다)
2. **`quiz.ts` 를 그대로 재사용합니다.** 4지선다를 새로 짜면 이미 고쳐놓은 사고
   (뜻이 같은 보기 · 한자 겹침 · 정답이 둘)를 처음부터 다시 밟습니다
3. **개인 점수를 화면에 띄우지 않습니다.** 점수는 **팀 단위**, 그것도 **맞힌 사람 수**로 셉니다

---

## 0단계 · 관리자 권한과 화면 기반

### [x] L0. 관리자 칸 — **마이그레이션 1개**

| | |
|---|---|
| **왜** | `/live` 는 **사장님 계정(`tass`)에서만** 보여야 합니다. 회원 화면에는 안 나옵니다 |
| **★ 동적으로** | 코드에 아이디를 박지 않습니다. **DB에서 켜고 끄고**, 관리자를 바꿔도 **다시 배포하지 않습니다** |
| **어디** | `supabase/migrations/20260810010000_admin.sql` (새 파일) |
| **뭘** | ① `profiles` 에 `is_admin boolean not null default false` ② `is_admin()` 판정 함수 ③ `my_profile()` 다시 만들기 |
| **★ 자기를 관리자로 못 만듭니다** | 마이그레이션 12가 수정 권한을 `email`·`phone` **두 칸에만** 줍니다. **새 칸은 아무도 못 고칩니다.** 따로 할 일이 없습니다 |
| **★ 화면 코드가 거의 안 늡니다** | `my_profile()` 이 프로필 **한 줄을 통째로** 돌려줍니다. 칸을 더하면 화면이 저절로 받습니다 |
| **★ `my_profile()` 을 다시 만드는 이유** | 반환 형식이 `public.profiles` 인데 그 표에 칸이 하나 늘어납니다. 새 파일에서 다시 만들어야 **최종 정의가 가장 나중 파일에** 오고, `09-handoff.md` 의 표도 같이 고쳐야 합니다 |
| **⚠ 두 번 연속 실행** | `add column if not exists` · `create or replace` 로 씁니다. **`db:push` 를 두 번 돌려 확인**합니다 — 두 번째 실행에서만 터지는 것이 있습니다 |
| **★ `tass` 를 마이그레이션에 적지 않습니다** | 누가 관리자인지는 **자료이지 구조가 아닙니다.** 적어두면 관리자를 바꿀 때마다 배포해야 해서 "동적" 의 반대가 됩니다 |
| **켜는 법** | Supabase SQL Editor 에서 한 줄:<br>`update public.profiles set is_admin = true where lower(username) = 'tass';` |
| **확인** | `select username, is_admin from public.profiles where is_admin;` |
| **기록** | 2026-08-10 · `20260810010000_admin.sql` 새로 만듦. `is_admin` 칸 + `is_admin()` + `my_profile()` 다시 만듦. **`db:push` 두 번 연속 통과.** `09-handoff.md` 함수 표에도 23번 추가. ⚠ **아직 아무도 관리자가 아닙니다** — SQL Editor 에서 `update` 한 줄을 실행해야 켜집니다 |

### [x] L0-1. 화면에서 관리자만 통과

| | |
|---|---|
| **어디** | `web/lib/useAuth.ts` 의 `Profile` 타입에 `is_admin` · `web/components/Nav.tsx` · `web/app/live/page.tsx` |
| **뭘** | ① 메뉴에 `/live` 를 **관리자에게만** 보여줍니다 ② 관리자가 아니면 `/live` 를 열어도 안내만 |
| **⚠ 못 받아온 것과 "아니다" 는 다릅니다** | `useAuth.ts` 에 이미 `profileFailed` 가 있습니다. 신호가 끊겨 프로필을 못 받았을 때 **"관리자가 아니다" 로 읽으면 안 됩니다** — 모임 도중에 화면이 닫힙니다. 못 받았으면 **이전 값을 그대로** 씁니다 |
| **⚠ 이게 지키지 못하는 것** | 기술을 아는 사람이 **주소를 직접 쳐서** 여는 것. 다만 `/live` 가 읽는 `v_words` 는 **이미 anon 에게 열려 있어** 새로 새는 정보가 0입니다. 모임 진행용으로는 충분합니다.<br>**나중에 팔 때**는 `/live` 가 저장이나 유료 자료를 다루게 되므로 **서버(RLS·함수)에** `is_admin()` 을 걸어야 합니다 (💰 칸 참고) |
| **기록** | 2026-08-11 · `Profile` 에 `is_admin` 추가 · `useAuth()` 가 `isAdmin` 을 내보냄 · `Nav.tsx` 에 `ADMIN_ITEMS`(모임 · 모니터 아이콘)를 관리자일 때만 이어붙임 · `app/live/page.tsx` 새로 만듦(문지기만, 화면은 L1). **못 받아온 것과 "아니다" 를 다르게 말합니다** — `profileFailed` 면 "계정을 확인하지 못했습니다", 진짜 아니면 "모임 진행용 화면입니다". `isAdmin` 은 프로필을 못 받아와도 이전 값을 그대로 써서 모임 중에 화면이 닫히지 않습니다. `npm run build` 통과(화면 10개) |

---

## 1단계 · 화면 기반

### [x] L1. `/live` 화면 뼈대

| | |
|---|---|
| **어디** | `web/app/live/page.tsx` (새 파일) |
| **뭘** | 단어 하나를 **3단계로 공개** — ① 한자만 → ② +병음(+스피커) → ③ +뜻·예문 |
| **★ 전체 덮기** | `fixed inset-0 z-50`. **`layout.tsx` 를 한 글자도 안 고칩니다** — 이러면 `max-w-5xl` 도 하단 메뉴(`z-20`)도 무력화됩니다.<br>*(라우트 그룹 레이아웃은 루트를 대체하지 않고 **중첩**돼서 소용없습니다)* |
| **★ 글자 크기** | **`px` 를 쓰면 안 됩니다.** TV 해상도를 모릅니다 — 4K에서 `px` 로 정하면 **절반 크기**로 보입니다.<br>한자 `clamp(120px, 22vmin, 420px)` · 병음 `clamp(32px, 6vmin, 110px)` · 뜻 `clamp(28px, 5vmin, 96px)` |
| **★ 한 화면에 단어 하나** | 목록·표 금지. 세 줄 넘기지 않기 |
| **자료** | **`getWords()` 하나만** 부릅니다. `useStore` 를 쓰면 안 됩니다 — 단어·진도·요약을 `Promise.all` 로 묶어서 **하나만 실패해도 셋 다 실패**합니다. 진도는 필요 없습니다 |
| **키보드** | `Space`·`→` 다음 · `←` 이전 · `Enter` 정답 한번에 · `F` 전체화면 · `Esc` 나가기 |
| **⚠ 키 반복** | `if (e.repeat) return` **필수.** 스페이스를 누르고 있으면 브라우저가 **초당 30번** 반복해 카드가 우수수 넘어갑니다. 기존 300ms 잠금(`study/page.tsx`)도 같이 |
| **마우스** | 큰 버튼 3개(이전/공개/다음)를 **반투명**으로, 3초 안 움직이면 사라지게 |
| **밝기** | 기본 **밝은 화면**(프로젝터는 검정을 못 만듭니다) + 뒤집기 버튼 |
| **기록** | 2026-08-11 · `app/live/page.tsx` 에 `LiveScreen` 추가. 3단계 공개 · `fixed inset-0 z-50` · 글자는 전부 `clamp()` · `getWords()` 하나만 부름 · 키 6개(`Space`·`→`·`←`·`Enter`·`F`·`Esc`) · `e.repeat` 무시 + 300ms 잠금 · 버튼 3초 뒤 사라짐 · 밝게/어둡게. **만들면서 직접 심었다가 고친 것 둘**: ① `setStep` 갱신 함수 **안에서** `setAt` 을 부름 → React 가 갱신 함수를 두 번 부르면 **단어가 두 칸씩 뜁니다**. `{at, step}` 한 덩어리로 바꿈 ② `opacity-40` 과 `opacity-0` 을 같은 곳에 붙임 → **Tailwind 는 뒤에 쓴 것이 이기지 않습니다**(만들어진 CSS 차례로 이김). 늘 40이 남아 안 사라짐. 삼항으로 하나만 내보내게 고침. `npm run build` 통과 |

### [x] L2. `/live` — 들어올 때만 확인하고, 모임 중에는 안 쫓아냄

| | |
|---|---|
| **⚠ 앞뒤가 안 맞던 것** | 처음 기획에는 "`/live` 는 로그인 없이" 라고 적혀 있었습니다. 그런데 **로그인을 안 하면 관리자인지 알 수가 없습니다**(L0-1). 둘이 부딪힙니다 |
| **어떻게 푸나** | **들어올 때는 로그인이 필요합니다**(관리자 확인). **들어온 뒤에는 안 쫓아냅니다** |
| **왜 안 쫓아내나** | 토큰이 **1시간이면 만료**되고 갱신에 인터넷이 필요합니다. **2시간 모임이면 반드시 그 구간에 들어갑니다.** 지금 `AuthGate` 는 로그아웃 신호를 받으면 로그인 화면으로 옮기는데, 그러면 **10명 앞에서 모임이 끊깁니다** |
| **어디** | `web/components/AuthGate.tsx` — `usePathname()` 이 `/live` 면 **쫓아내지만 않습니다**(화면은 그대로 둡니다) |
| **★ 시작하면 서버를 안 부릅니다** | 묶음을 시작할 때 단어를 화면에 통째로 복사하고, 그 뒤로 서버를 **한 번도** 안 부릅니다. 세션이 끊겨도 모임은 끝까지 굴러갑니다 |
| **안전한가** | `/live` 가 읽는 `v_words` 는 **이미 anon 에게 열려 있어** 새로 노출되는 것이 0입니다 |
| **기록** | 2026-08-11 · `AuthGate.tsx` 에 `cameInBefore` ref — 한 번이라도 들어온 적이 있고 지금 `/live` 면 로그인 화면으로 안 바꿉니다. **AuthGate 만 고치면 반쪽이었습니다** — 토큰이 만료되면 프로필도 같이 비어서 `/live` **페이지 자체가** "관리자가 아닙니다" 로 바뀝니다. 결국 같은 자리에서 모임이 끊깁니다. `live/page.tsx` 에도 `admitted` ref 를 둬서 한 번 들여보내면 안 내보냅니다. 들어올 때는 그대로 로그인·관리자 확인을 받습니다 |

### [x] L3. 큰 화면용 글자

| | |
|---|---|
| **어디** | `web/app/globals.css` |
| **뭘** | `/live` 전용 — **큰 병음**(자간·줄높이). 5m 에서 **제일 먼저 안 보이는 게 성조 기호**입니다 |
| **★ 지뢰** | **`@theme` 을 새로 쓰지 않습니다.** 미디어 쿼리 안에 넣으면 낮에도 밤에도 어두운 화면이 됩니다. `:root`/클래스에서 **변수만** 다시 정의 |
| **`.han`** | 그대로 씁니다(필수). 다만 굵기는 **`font-bold` 까지만** — 900을 쓰면 브라우저가 가짜로 굵게 만들어 큰 글자에서 획이 뭉갭니다 |
| **기록** | 2026-08-11 · `globals.css` 의 `@layer utilities` 에 `.live-pinyin`(자간 0.08em · 줄높이 1.5 · 굵기 500)과 `.live-han`(`--font-han` · 굵기 **700 고정** · 줄높이 1) 추가. `@theme` 은 안 건드렸습니다. `/live` 의 한자·병음이 이걸 씁니다. 자간을 넓힌 이유는 멀리서 보면 음절이 한 덩어리로 뭉치기 때문이고, 줄높이를 넉넉히 준 이유는 성조 기호가 글자 위로 튀어나와 잘리기 때문입니다. **5m 확인은 사람 눈이 필요합니다(L17)** |

### [x] L4. 서비스 워커 판 올리기

`web/public/sw.js` 의 `VERSION`. 안 올리면 **배포해도 옛 화면이 뜹니다**(지뢰 표).

**기록** · 2026-08-11 · `v1` → `v2`. 왜 올리는지 주석도 같이 남겼습니다.

---

## 2단계 · 게임 — 여기까지 하면 1회차를 실제로 돌릴 수 있습니다

### [x] L5. ① 성조 체조 — **첫 게임**

| | |
|---|---|
| **왜 이것부터** | 화면이 할 일이 제일 적고(단어·뜻만 크게) **오프라인 이득이 제일 큽니다** |
| **규칙** | 단어와 뜻을 띄우고 짚은 글자가 몇 성인지 **몸으로**. "하나 둘 셋"에 전원 동시.<br>1성=팔 옆으로 뻗어 유지 · 2성=아래→위 · 3성=내렸다 올림(V) · 4성=위→아래 내려침 · 경성=어깨 으쓱 |
| **재사용** | `pickToneDeck` · `toneVariants` (이미 있습니다) |
| **★ 소리** | **TTS 는 정답 공개 뒤에만.** 먼저 틀면 소리가 곧 정답입니다 |
| **기록** | 2026-08-11 · `app/live/ToneGym.tsx`. 한 라운드 10문제 · `pickToneDeck` 재사용 · 묻는 글자만 진하게(나머지 25%) · `하나 둘 셋` 3초 카운트 뒤 자동 공개 · 정답에 성조 이름과 **몸 동작 설명**. TTS 는 공개된 뒤에만 자동으로 한 번. **세는 중에는 Space 를 안 받습니다** — 아직 아무도 답을 안 했는데 정답이 뜨면 그 문제는 통째로 날아갑니다. 게임이 여덟 개 더 붙을 자리라 공용 껍데기(`shell.tsx`)를 먼저 떼어냈습니다: `LiveFrame`(덮기·밝기·전체화면·나가기) · `useLiveKeys`(`e.repeat` 무시 + 300ms 잠금) · `useAwake`(3초 뒤 버튼 감춤) · `BIG`(clamp 글자 크기). L1 의 단어 넘기기는 `Cards.tsx` 로 옮겼고 `/live` 는 게임 고르는 화면이 됐습니다 |

### [x] L6. 팀 점수판

| | |
|---|---|
| **★ 개인 점수 없음** | 화면에 개인 이름 옆 숫자를 **절대** 안 띄웁니다 |
| **★ 팀 점수 = 맞힌 사람 수** | 한 명이 다 맞혀도 1점, 넷이 맞히면 4점 → **옆 사람을 가르치는 게 팀에 이득**이 됩니다 |
| **키** | `1`/`2` 득점 · `Backspace` **방금 점수 취소** (없으면 잘못 셌을 때 진행이 멈춥니다) |
| **팀** | 2팀 고정 (6명=3+3 · 10명=5+5) |
| **기록** | 2026-08-11 · `shell.tsx` 의 `useTeams` + `Scoreboard`(왼쪽 위, 늘 보임). 점수를 넘긴 게임에만 `1`·`2`·`Backspace` 가 붙습니다. **점수 키에는 300ms 잠금을 안 겁니다** — 네 명이 맞혀서 연달아 네 번 치는데 잠그면 두 번만 들어갑니다. 점수는 게임 하나가 아니라 **판 전체**를 따라가도록 `/live` 본체가 들고 있습니다. `ToneGym` 이 쓰던 `Backspace`(게임 고르기)는 점수 취소에 양보하고 버튼만 남겼습니다. **또 같은 함정**: `setHistory` 갱신 함수 **안에서** `setScores` 를 불렀습니다 — 두 번 실행되면 한 번 눌렀는데 2점이 깎입니다. 점수와 기록을 한 덩어리 상태로 합쳐서 고쳤습니다. 이 함정은 이번 회차에만 **세 번**(L1·L5·L6) 나왔습니다 |

### [x] L7. ⑦ 다 같이 살리기 (협동)

| | |
|---|---|
| **왜 중요** | **모든 판의 마지막**입니다. 진 팀도 같은 편으로 끝내고 쉬는 시간에 들어갑니다 |
| **규칙** | 경쟁 없음. 8문제 중 6개면 통과, 목숨 3개.<br>화면이 답할 사람을 **무작위 지목** → 팀 전체 **15초 상의** → **마지막 말은 지목된 사람** |
| **★** | 지목은 **화면이** 합니다. 사람이 지목하면 "왜 나만" 이 나옵니다. 한 바퀴 돌 때까지 중복 없이 |
| **기록** | 2026-08-11 · `app/live/Coop.tsx`. 8문제·통과 6개·목숨 3개·15초. 지목은 이름을 섞어 한 바퀴 돌 때까지 중복 없이(사람이 문제보다 적으면 바퀴마다 새로 섞음). **팀 점수판을 일부러 안 넘겼습니다** — 여기서까지 팀을 나누면 진 팀이 진 채로 끝납니다. 판정은 진행자 손(`O`/`X`). 참여자 이름 입력을 같이 만들었습니다(`NameBox`, 브라우저에만 저장, 서버로 안 보냄). **여기서 하나 걸렸습니다**: 이 화면은 키를 `window` 에서 통째로 받아 `preventDefault` 하는데, 그러면 **이름 칸에 글자를 못 칩니다** — `Backspace` 로 안 지워지고 `1`·`2` 는 점수가 오르고 `f` 는 아예 안 써집니다. `useLiveKeys` 가 `INPUT`·`TEXTAREA`·`contenteditable` 에서 온 키를 그냥 흘려보내게 고쳤습니다 |

### [x] L8. ② 빈칸 채우기

`blankSentence` 재사용. 보기 4개(한자+병음)에 A~D. 10초. **8초에 자동 힌트**.

**기록** · 2026-08-11 · `app/live/Blank.tsx`. 8문제 · 10초 · 2초 남았을 때 뜻을 힌트로.
**4지선다를 새로 짜지 않고 `makeQuiz(word, pool, 2)` 를 그대로 불렀습니다** — 인덱스 2 면
빈칸 문제가 나오고, 이미 고쳐둔 오답 규칙(뜻이 같은 보기 · 한자 겹침 · 예문에 이미 보이는 글자)을
통째로 물려받습니다. `makeQuiz` 는 조건이 안 맞으면 다른 형식으로 내려가므로 `kind === 'blank'`
인 것만 씁니다. 소리는 정답 공개 뒤에만 — 예문을 먼저 읽어주면 가린 자리가 소리로 들립니다.

### [x] L9. ③ 귀로 잡기 (듣기)

| | |
|---|---|
| **뭘** | 한자를 **감춘 채** TTS 2회. **보기는 한자 4개** — 병음을 보기로 내면 소리가 곧 답입니다 |
| **상급** | 보기 없이 **종이에 병음 받아쓰기**, 옆 팀과 교환 채점 |
| **⚠** | 시작 화면에서 TTS 를 한 번 재생해 **목소리가 없는 기기면 이 게임을 목록에서 뺍니다** |
| **기록** | 2026-08-11 · `app/live/Listen.tsx`. 8문제 · 문제가 바뀌면 소리 2회 자동 · 다시 듣기 1회(`R`). 보기는 `makeQuiz(word, pool, 3)` 이 내주는 **한자 4개**(`pick-zh`) — 인덱스 3 은 병음(4의 나머지 1)도 빈칸(3의 나머지 2)도 아니라 뜻→한자로 떨어집니다. **보기에 병음을 안 붙였습니다** — 붙이면 들은 것을 눈으로 찾는 일이 되어 듣기 연습이 아니게 됩니다. 상급(보기 감추고 종이에 병음 받아쓰기)은 버튼으로 켭니다. `useCanSpeak()` 가 `false` 면 시작 화면 목록에서 이 게임을 빼고 이유를 한 줄 띄웁니다. `null`(확인 중)일 때는 그대로 보여둡니다 — 처음 값만 보고 없다고 하면 되는 기기에서도 사라집니다. 게임을 나갈 때 예약해둔 소리를 지웁니다(안 지우면 나가도 떠듭니다) |

### [x] L10. ⑧ 한자 가족 열기

`sharingHanzi` 재사용. 가운데 글자 하나, 그 글자가 든 단어 8개가 가려진 칸.

**기록** · 2026-08-11 · `app/live/Hanzi.tsx`. 씨앗은 **한 글자 단어**만 씁니다 — 두 글자를
주면 두 글자의 가족이 섞여서 "가운데 글자 하나" 규칙이 깨집니다. `sharingHanzi` 가
빈도순 정렬과 한자 중복 제거(`为` 동사/개사)를 이미 하고 있어 그대로 받았습니다.
**칸을 여는 것이 곧 그 팀의 득점**이고 차례가 넘어갑니다 — 진행자는 번호만 누르면 됩니다.
`Backspace` 는 칸과 점수를 **같이** 되돌립니다(하나만 되돌리면 점수가 어긋납니다).
**키가 부딪혔습니다**: 칸이 `1`~`8` 인데 팀 득점도 `1`·`2` 라 첫 두 칸을 못 엽니다.
`LiveFrame` 에 `teamKeys` 스위치를 달아 이 게임만 점수 키를 끄고 점수판은 그대로 띄웁니다.

### [x] L11. ⑤ 빨리 손들기

한자만 크게. 먼저 손 든 사람이 뜻을 말로.
**★ 맞힌 사람은 다음 2문제(10명이면 3문제) 쉽니다.** 화면이 쉬는 사람 이름을 띄웁니다.

**기록** · 2026-08-11 · `app/live/Hands.tsx`. 12문제 · 8초에 **병음만** 힌트로(뜻은 아직).
맞힌 사람은 화면의 이름을 눌러 표시하고, 그때부터 `names.length >= 10 ? 3 : 2` 문제를 쉽니다.
쉬는 사람은 위에 이름이 뜹니다 — **손을 안 드는 것이 눈치가 아니라 규칙이 되게** 하려고요.
이름을 안 적었으면 게임은 그대로 돌아가고 안내만 한 줄 띄웁니다.

### [x] L12. ④ 설명해서 맞히기

| | |
|---|---|
| **규칙** | 한 명이 **화면에 등 지고** 앉음. 나머지가 **한국어로** 설명. 60초 |
| **금지** | 그 한자·병음·한국어 뜻 그대로 |
| **★ 허용** | 한국어 설명 · 몸짓 · **화면이 같이 띄우는 한자 가족** · 자료에 있는 예문 읽어주기<br>→ **중국어를 새로 지어낼 필요가 없습니다** (프로젝트 규칙) |
| **기록** | 2026-08-11 · `app/live/Explain.tsx`. 60초 동안 몇 개나 · `Space` 맞힘 · `→` 통과.
시작 전에 **어느 팀 차례인지** 먼저 고르고, 맞힐 때마다 그 팀에 1점씩 들어갑니다.
**금지어를 화면이 지킵니다** — 그 한자·병음·뜻을 상자에 따로 띄웁니다. 진행자가 스무 단어의
금지어를 외우고 있을 수는 없습니다. 써도 되는 재료(한자 가족 3개 `sharingHanzi` · 자료의 예문)도
같이 띄웁니다 — **중국어를 새로 지어낼 사람이 이 프로젝트에 없습니다.**
등을 진 사람은 화면을 못 보므로 화면에는 답을 다 적어도 됩니다 |

### [x] L13. ⑥ 성조 릴레이 (마이크)

| | |
|---|---|
| **재사용** | `ToneSpeak` · `pickSpeakDeck` · `pitch.ts` (이미 있습니다) |
| **★ 진행자 판정 우선 키** | **마이크 판정은 아직 사람 손으로 검증이 안 됐습니다**(`09-handoff.md`). 10명 앞에서 오판정이 나면 그 라운드가 통째로 무너집니다 |
| **자리** | 매회 **맨 뒤 13분** 고정. 한 명씩 하는 형식이라 앞에 두면 지루함이 퍼집니다 |
| **★ 강제 금지** | 자원자만. 남 앞에서 발음하는 것이 가장 무서운 일입니다 |
| **기록** | 2026-08-11 · `app/live/Relay.tsx`. `ToneSpeak` 컴포넌트와 `pickSpeakDeck` 을 그대로 씁니다(한 글자 · 경성 제외). **마이크 판정을 점수에 쓰지 않습니다** — 참고로 한 줄 띄우기만 하고 점수는 진행자가 `O`/`X` 로 줍니다. 팀은 번갈아 나옵니다. 화면이 사람을 **지목하지 않습니다** — 자원자만 받습니다 |

---

## 3단계 · 진행 도구

### [x] L14. 회차·범위 고르기

973단어를 **빈도로 6등분**(회차당 약 162개). `frequency` 칸이 이미 있습니다.
시작 화면에서 **1~6회차 중 고르기** + 게임 고르기.

**기록** · 2026-08-11 · 시작 화면에 `전체 · 1~6회차` 단추와 지금 몇 단어인지 표시.
`getWords()` 가 **이미 빈도순**으로 주기 때문에 따로 정렬하지 않고 잘라 씁니다.
**게임에는 잘라낸 범위만 넘깁니다** — 전체를 넘기고 게임 안에서 자르면 게임마다
자르는 규칙이 갈립니다.
⚠ 기획의 "6회차는 1~5회에서 놓친 것으로만" 은 **아직 안 됩니다** — 놓친 것을
회차 사이에 들고 있을 자리가 없습니다(L15 는 그날 것만 모읍니다).

### [x] L15. 마무리 화면 — 오늘 놓친 것

한 화면에 모아 띄웁니다. 참여자가 **폰으로 찍어갑니다**. 다음 회 처음 10분이 여기서 시작합니다.

**기록** · 2026-08-11 · `shell.tsx` 의 `useMissed()` 가 모으고, `/live` 의 `마무리` 항목이 한 화면에 띄웁니다.
**아는 자리는 자동으로 담습니다** — ⑦ 협동의 X 판정 · ④ 설명의 통과 · ⑥ 릴레이의 X ·
⑧ 한자 가족에서 **끝까지 안 열린 칸**. 나머지 게임(①②③⑤)은 진행자가 `M` 을 누릅니다.
같은 단어는 두 번 담지 않습니다.
글자 크기를 개수에 따라 줄입니다(12개 · 24개 기준) — **찍어가는 화면에서 잘려나간 줄은 없는 것과 같습니다.**
서버에 안 보냅니다.

### [x] L16. 오프라인 대비 소리

`web/lib/speak.ts` — **로컬 목소리를 먼저** 고르기.
크롬/엣지의 중국어 목소리 상당수가 **클라우드**라 인터넷이 느리면 끊깁니다.

**기록** · 2026-08-11 · `chineseVoice()` 가 `localService` 를 보고 고릅니다.
순서는 **기기 안 zh-CN → 기기 안 아무 중국어 → 클라우드 zh-CN → 나머지**.
기기 안에 중국어 목소리가 하나도 없으면 그때는 클라우드라도 씁니다(안 쓰면 소리가 아예 없어집니다).
품질이 조금 떨어져도 **끊기지 않는 쪽**을 고른 것입니다 — 혼자 쓸 때는 다시 누르면 그만이지만,
모임에서는 열 명이 보고 있고 듣기 게임은 소리가 곧 문제입니다. 회의실 와이파이는 대개 느립니다.
⚠ **어느 목소리가 잡히는지는 기기마다 다릅니다.** 사장님 노트북에서 실제로 확인해야 합니다(L17).

---

## 4단계 · 확인

### [ ] L17. 사장님이 TV 앞에서 (기계가 못 함) ← **여기만 남았습니다**

**1차 확인 (2026-08-11) 에서 나온 것 — 전부 고쳤습니다**

| 무엇 | 왜 그랬나 | 어떻게 고쳤나 |
|---|---|---|
| 시작 화면의 **이름 칸과 아래 버튼이 안 보임** | 껍데기가 화면을 꽉 채운 채 **가운데 정렬만** 해서, 내용이 길면 위아래가 그냥 잘려나갔습니다 | 틀을 **세 줄(위·가운데·아래)** 로 나누고 가운데만 굴러가게. 시작 화면 간격·글자도 줄임 |
| **`나가기`·`전체화면` 이 진행 버튼과 같은 줄** | 아래 한 줄에 다 몰아넣었습니다. `다음` 옆에 `나가기` 가 붙어서 잘못 누르면 모임이 통째로 닫힙니다 | 화면 버튼은 **오른쪽 위**로 옮기고 작게·흐리게. 아래는 진행 버튼만 |
| 성조 체조의 **팔 움직임 설명이 헷갈림** | 매 문제 정답 밑에 `아래에서 위로 올려요` 같은 글을 띄웠습니다 | 지웠습니다. **성조 이름만** 띄웁니다. 동작은 진행자가 첫 판에 한 번 알려주면 됩니다 |

**2차 요청 (2026-08-11)**

| 무엇 | 어떻게 |
|---|---|
| **규칙 설명 화면** | 게임에 들어가기 전에 한 번 띄웁니다. 한 줄 요약 + 순서 4단계 + 진행자 키. **진행자가 읽어주는 화면**입니다 — 참여자는 규칙을 모른 채 앉아 있고 진행자도 여덟 개를 다 외우고 있을 수 없습니다. `Space` 한 번에 넘어가니 아는 게임이면 바로 시작합니다. 규칙이 없는 것(단어 넘기기·마무리)은 바로 들어갑니다 |
| **성조 힌트** | `H` — **넷 중 둘로 좁힙니다** (`2성 아니면 4성`). ★ 병음은 힌트로 못 씁니다. `ā á ǎ à` 의 기호가 곧 답입니다. **정답이 늘 왼쪽에 오지 않게** 자리를 섞습니다 — 안 그러면 두 번째 문제부터 왼쪽이 곧 답입니다 |
| 한국어 뜻 | **이미 나오고 있었습니다.** 성조와 상관없는 정보라 답이 안 샙니다 |

**3차 확인 (2026-08-11) — 시작 화면을 다시 짰습니다**

| 무엇 | 왜 그랬나 | 어떻게 고쳤나 |
|---|---|---|
| **좁은 창에서 머리줄이 겹침** | 위 줄이 `justify-between` 한 줄이라 점수판 위로 화면 버튼이 올라탔습니다 | 줄이 **접히게**(`flex-wrap`) 바꾸고 순서를 정했습니다. 좁으면 진행 표시가 아래로 내려갑니다 |
| **시작 화면이 정신없음** | 회차 단추 · 게임 단추 · 안내문 세 덩이가 그냥 세로로 쌓여 있었습니다. **무엇을 먼저 정해야 하는지가 화면에 없었습니다** | **번호 붙은 네 칸**으로 세웠습니다 — 1 오늘 범위 · 2 참여자 · 3 팀 점수 · 4 무엇을 할까. 칸마다 **왜 필요한지**를 제목 옆에 답니다 |
| **`1팀 0 2팀 0` 이 뭔지 모름** | 게임 밖 시작 화면에도 점수판만 덩그러니 떠 있었습니다 | 시작 화면에서는 **점수판을 머리줄에서 빼고**, 3번 칸에서 팀 나누는 법·`1`·`2` 로 세는 법·`Backspace` 취소를 같이 설명합니다 |
| **참여자 이름을 왜 적나** | 이유를 아무 데도 안 적었습니다 | 제목 옆에 적었습니다 — **⑦ 무작위 지목과 ⑤ 쉬는 차례**에 씁니다. 기록용이 아니고, **안 적어도 나머지 게임은 다 됩니다** |
| **게임 차례가 뒤죽박죽** | ⑧ 이 ⑦ 앞에 있었습니다 | ①~⑧ 번호순으로 바로잡았습니다 |
| 반응형 | 게임 단추가 `min-w-[30vmin]` 라 창 크기에 따라 제멋대로 접혔습니다 | **`grid`** 로 바꿨습니다 — 좁으면 1줄, 보통 2줄, 넓으면 3줄 |
| 진행자 키 안내 | 시작 화면에 두 줄로 길게 있었는데, **규칙 화면에 게임별로 이미 있습니다** | 시작 화면에서 뺐습니다 |
| **아직도 머리줄과 겹침 (4차)** | 머리줄 문제가 아니었습니다. **가운데 칸에 `items-center` 와 `overflow-y-auto` 를 같이 걸어서**, 내용이 칸보다 커지면 위쪽이 잘려나가고 그 위로는 스크롤이 안 올라갔습니다 | 굴리는 칸과 가운데 맞추는 칸을 **나눴습니다**. 바깥이 굴러가고 안쪽이 `min-h-full` 로 가운데를 맞춥니다. 짧으면 가운데, 길면 위에서부터 끝까지 굴러갑니다. 머리줄·아래줄에 **경계선**도 그어서 자리가 눈에 보이게 했습니다 |

- [ ] **5m 뒤에서 병음 성조 기호가 읽히나** ← 제일 중요. 안 읽히면 성조 게임이 성립 안 합니다
- [ ] 한자 크기 · 밝기(프로젝터/TV)
- [ ] 스페이스를 눌러도 카드가 안 넘어가나
- [ ] TTS 가 실제로 나오나
- [ ] 진행자 키 5개가 손에 익나

### [x] L18. 문서 갱신

`09-handoff.md` 에 `/live` 와 **모임 전 확인 목록** 추가.

**기록** · 2026-08-11 · 머리말 날짜와 상태 표에 `/live` 추가.
"지금 할 것" 절을 **L17(사장님이 TV 앞에서 볼 목록)** 으로 바꾸고,
`/live` 사용법(회차 → 이름 → 게임)과 **진행자 키 표**, `/live` 가 일부러 안 하는 것 네 가지를 적었습니다.
**지뢰 표에 이번 회차에 밟은 세 가지**를 추가: 갱신 함수 안에서 다른 상태 바꾸기(세 번 밟음) ·
같은 성질의 Tailwind 클래스 둘 붙이기 · `window` 에서 키를 받고 `preventDefault` 해서 입력 칸이 막히는 것.

---

## 💰 유료화로 넘어가야 하는 것 (지금은 안 함)

| | 언제 필요 | 얼마 |
|---|---|---|
| **`/live` 를 서버에서 막기** | 팔기 시작해서 `/live` 가 저장이나 유료 자료를 다루게 될 때. 지금은 **화면에서만** 막습니다 — 주소를 직접 치면 열립니다 | 코드 작업 (돈은 안 듦) |
| **Vercel Pro** | **Hobby 요금제는 약관상 상업적 사용 금지입니다.** "돈 받고 판다" 가 현실이 되는 순간 | **$20/월** |
| Supabase Pro | 지금은 무료 안에 넉넉합니다. 참여자가 각자 계정을 쓰게 되면 다시 계산 | $25/월~ |
| AI 분석 (WBS 14번) | Anthropic API. 기록이 쌓인 뒤 | 사용량만큼 |

**지금 만드는 것은 전부 무료 요금제 안입니다.** 위는 팔기 시작할 때 봅니다.

---

## 진행 규칙 (사장님 지시 · 2026-08-10)

1. **항목 하나 끝날 때마다 커밋하고 `dev` 에 푸시.** `main` 에는 올리지 않습니다
2. **오류·문제·유료화 필요는 아래에 체크만 하고 다음으로 넘어갑니다**
3. **다음 항목이 막힌 것에 딸려 있으면 같이 체크하고 건너뜁니다**
4. **같은 오류를 3번 고쳐도 계속 나면 거기서 멈추고 체크**합니다. 나중에 몰아서 디버그
5. 목록이 다 돌면 체크해둔 것을 다시 시험합니다

---

### [x] L19. 회차별 판 짜기 — **빠뜨렸던 것**

| | |
|---|---|
| **무엇을 빠뜨렸나** | 기획서([13-offline-game.md](13-offline-game.md))의 **회차별 초점 표**를 화면에 안 올렸습니다. 회차는 **단어 범위만** 갈랐고, 게임 목록은 여섯 주 내내 똑같이 열 개가 나왔습니다 |
| **왜 문제인가** | 사장님이 보고 **"매주 같은 게임을 하는 거냐, 6주면 지루하지 않겠냐"** 고 물었습니다. 기획은 그렇지 않은데 **화면만 보면 그렇게 보였습니다.** 기획서에 있는 것이 화면에 없으면 없는 것입니다 |
| **뭘** | 회차를 고르면 **그날 판**이 순서대로 뜹니다 — 판1 · 판2 · 판3 · 마무리(⑦) · 맨 뒤(⑥). 초점도 같이 적습니다 |
| **판** | 1회 성조 감각 → 2회 성조 짝 → 3회 듣기 시작 → 4회 헷갈리는 발음 → 5회 문장 단위 → 6회 종합 |
| **그 밖에** | 판을 바꾸고 싶으면 아래 전체 목록에서 골라도 됩니다. 강제하지 않습니다 |
| **기록** | 2026-08-11 · `PLANS` 표 + 시작 화면 4번 칸. 목소리가 없는 기기에서는 판 안의 ③ 이 눌리지 않게 흐리게 둡니다(빼버리면 판 번호가 어긋납니다) |

---

## 🔴 막힌 것 (3번 고쳐도 안 됨 — 나중에 디버그)

*(아직 없습니다)*

## 🟡 문제 (돌아가긴 하는데 손봐야 함)

| | |
|---|---|
| ~~**`/live` 를 사람이 한 번도 안 봤습니다**~~ | **2026-08-11 사장님이 봤습니다.** ① 성조 체조까지 확인 — 소리·문제 모두 정상. 나머지 게임은 아직 |
| **마이크 판정(⑥)** | 여전히 사람 손으로 검증이 안 됐습니다. 그래서 L13 은 판정을 점수에 안 씁니다. 검증되면 그때 자동 채점으로 올리면 됩니다 |
| **소리 두 번 재생 간격(③)** | `speak()` 이 앞의 소리를 끊는 구조라, 두 번 들려줄 때 2초 고정으로 띄웠습니다. 긴 단어에서 첫 번째가 잘릴 수 있습니다. 발화 끝 신호를 쓰려면 `speak()` 을 고쳐야 합니다 |
| **`1`·`2` 키가 게임마다 다릅니다** | 보통은 팀 득점인데 ⑧ 한자 가족에서는 칸 번호입니다. `teamKeys` 로 갈랐지만, 진행자가 헷갈릴 수 있습니다. L17 에서 봐주세요 |

## ⏭ 딸려서 같이 건너뛴 것

*(아직 없습니다)*
